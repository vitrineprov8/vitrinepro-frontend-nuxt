# vitrinepro-frontend-v2 (HUNTRIA) — Guia para Claude

Frontend Nuxt 4 do produto de hunters. Backend: NestJS reutilizado em `../vitrinepro-bakend` (NÃO reescrever).

> ⚠️ **REGRA — documento vivo:** ler este arquivo antes de trabalhar e **atualizá-lo no mesmo commit** ao mudar estrutura, rotas, composables de API ou convenções. Contratos de API e endpoints do backend estão em `../vitrinepro-bakend/CLAUDE.md` (também documento vivo).

## Stack técnico
- **Nuxt 4.4** (Vue 3.5, `<script setup lang="ts">`, TS estrito) — dev na porta **4321**; backend NestJS na **3000**.
- **Estado:** Pinia 3 (`@pinia/nuxt`) em `app/stores/`. **Utils:** VueUse 13. **Ícones:** `lucide-vue-next`.
- **Estilo:** CSS scoped + tokens `var(--*)` de `app/assets/css/tokens.css`. **Sem Tailwind, sem lib de UI** — UI kit próprio em `app/components/ui/`.
- **HTTP:** só via `useApi()` (JWT + erros padronizados). Nunca `$fetch`/`axios` direto.
- **Renderização:** rotas públicas = SSR + SEO; `/app/**` = client-only com middleware `auth`.
- **Lint/typecheck:** ESLint 9 (`@nuxt/eslint`) + `vue-tsc`. Idioma da UI: **PT-BR**.
- **Estrutura:** `app/pages` (rotas) · `app/components` (+ `ui/`) · `app/composables` · `app/stores` · `app/layouts` (marketing/auth/app) · `app/middleware` · `app/types`.

## Setup inicial (uma vez)
```bash
# 1. Ativar os agentes (.claude/agents/ ainda não foi populado neste checkout)
mkdir -p .claude/agents && cp claude-setup/agents/*.md .claude/agents/
# 2. Instalar e rodar
npm install
npm run dev   # http://localhost:4321 (backend em :3000)
```

## Documentos-fonte (ordem de autoridade)
1. `../design-spec/00_MASTER_DESIGN_SYSTEM.md` — design system (tokens já implementados em `app/assets/css/tokens.css`)
2. `../design-spec/01..06_*.md` — spec de TODAS as telas, modais, drawers e fluxos (IDs: T01, T-C0x, T-H0x, T-T0x, T-E0x, P/M/A/N)
3. `PLANO_DESENVOLVIMENTO.md` — fases, checklist e **regras de negócio RN-\* + gaps de backend (B1..B16)**. É a fonte viva de regras: os arquivos `REGRAS_DE_NEGOCIO_VITRINEPRO.md` e `PROPOSTA_NEGOCIO_HUNTER.md` NÃO existem mais; suas regras foram consolidadas aqui (§BACKEND) e no código do NestJS.
4. `../plano-negocio-vitrinepro.html` — visão de negócio/marketing (contexto, não regras técnicas)
5. Verdade final de contratos de API: o código em `../vitrinepro-bakend/src/**` (cada módulo = controller + service + entities)

## Agentes (usar proativamente)
- `sprint-planner` — início de sessão: o que fazer agora, dependências de backend
- `nuxt-screen-builder` — implementar telas a partir das specs
- `api-integrator` — composables/tipos da API, divergências com o NestJS
- `design-system-guardian` — revisão visual/tokens antes de concluir
- `qa-reviewer` — revisão funcional/acessibilidade depois de concluir

## Convenções (resumo — detalhes nos agentes)
- TS estrito, `<script setup lang="ts">`, CSS scoped com tokens `var(--*)`, sem Tailwind, PT-BR.
- HTTP só via `useApi()`; estado global em Pinia (`app/stores/`); toasts via `useToast()`.
- Público = SSR + SEO; `/app/**` = client-only com middleware `auth`.
- Padrão de qualidade de referência: `app/pages/login.vue` e `app/components/ui/*`.
- Nunca permitir publicar vaga via PATCH; nunca esconder recurso bloqueado por plano (mostrar cadeado + Modal Upgrade).

## Comandos
`npm run dev` · `npm run build` · `npm run typecheck` · `npm run lint`

## Validação E2E (navegador) — evita re-derivar
Gotchas de ambiente e o runbook completo estão em `../vitrinepro-bakend/CLAUDE.md` §Ambiente. Resumo:
- **Validação é sempre no Chrome contra os servers locais do Andres** — regra permanente, nunca marcar `[x]` no plano só por leitura de código. Banco de dev é de teste (sem dados reais) — Claude pode criar/usar/apagar dados de teste livremente (contas via fetch programático, vagas, portfólio etc.), limpando o descartável e registrando em `../qa-test-accounts.json` o que tiver valor de reuso. Detalhe completo e a nota de transparência sobre criação de conta: `../vitrinepro-bakend/CLAUDE.md` §Ambiente. JWT em cookie sobrevive a restart.
- `nuxt typecheck`/`dev` **não rodam no sandbox** (node_modules do Windows). Rodar na máquina do Andres.
- **T-H08 validado E2E** (pool → consentimento → publicar vaga → submeter 3 passos → 201; reenvio → 409). Consentimento sem caixa de e-mail: token dev sai no console ou concede-se via `javascript_tool` no `POST /public/candidate-consent/:token`.
- Padrão p/ menus/dropdowns em tabelas com `overflow`: **teleportar p/ body com posição fixa** (ver `pages/app/hunter/candidatos.vue`) — senão o container clipa. Ao abrir modal a partir de um drawer, fechar o drawer (senão sobrepõem por z-index).
- Página pública de consentimento (B14): `pages/consentimento/[token].vue` (`layout:false`).
- Páginas públicas de token (B2/B7), mesmo padrão `layout:false`: `pages/redefinir-senha/[token].vue` e `pages/convite/[token].vue`. `T14`/`T15` (`recuperar-senha.vue`/`redefinir-senha/[token].vue`) já usam o backend real — o mock `import.meta.dev` foi removido de `stores/auth.ts`.
- Página pública de empresa (B6): `pages/empresa/[slug].vue` (layout padrão/marketing, com nav — diferente das páginas de token acima). Backend: `GET /empresas/:slug`. Tipo `CompanyVagaSummary` em `types/vaga.ts` (projeção enxuta de vaga, não confundir com `Vaga` completo — `VagaCard.vue` espera `Vaga` completo e não deve ser reusado aqui).
- **Persona/role (B1)**: `stores/auth.ts` tem `User.personas: Persona[] | null` (`'CANDIDATO'|'HUNTER'|'EMPRESA'`), `register(payload.persona?)` envia a persona escolhida no `/cadastro` (fluxo por e-mail) e `activatePersona(persona)` chama `PATCH /profile/me/personas` (idempotente no backend, erros engolidos — não bloqueia login). `pages/cadastro.vue` manda `persona: 'HUNTER'|'CANDIDATO'|undefined` no `auth.register(...)` em vez de só salvar em `localStorage`. `pages/auth/callback.vue` (fluxo OAuth) chama `auth.activatePersona(...)` **depois** do `loginWithToken` — só dá pra persistir a persona quando a conta OAuth já existe, então o `localStorage.getItem('vp_persona_choice')` continua guardando a escolha entre o clique em "Continuar com Google/LinkedIn" e a volta do callback. `localStorage.vp_last_workspace` continua só para navegação (qual workspace abrir), não é mais a fonte de verdade da persona — isso agora é `User.personas` no backend.
- **Chrome MCP bloqueia navegação para caixas de e-mail temporárias** (`inboxes.com`, `getnada.com` deram "Navigation to this domain is not allowed"). Para ver o e-mail renderizado (reset de senha, convite de time) o Andres precisa checar a caixa manualmente.
- **B2 validado E2E completo (2026-07-05):** `forgot-password` disparado de verdade pela UI para `testeia@getnada.com` → e-mail real recebido → Andres redefiniu a senha pelo link → confirmado. Nova senha em `../qa-test-accounts.json`.
- **B7 ainda falta validar ponta a ponta**: `/convite/[token]` renderiza certo pro estado de link inválido contra o backend real, mas falta testar um convite de verdade (precisa de conta plano TEAM/ENTERPRISE — não existe ainda em `qa-test-accounts.json`).
- **B6 validado E2E completo (2026-07-06)**: conta `isCompany` real (`andresempresa@getnada.com`, ver `qa-test-accounts.json`) confirmou `/empresa/andreshernandez9975` com dados reais e estado vazio de vagas; `/empresa/slug-inexistente` confirma o 404 real. Falta só ver a listagem `vagasAbertas` com uma vaga publicada de verdade.
- **B1 validado E2E completo (2026-07-06)**: `PATCH /profile/me/personas` testado via `javascript_tool` contra o backend real (`testeia@getnada.com`) — ativar HUNTER, idempotência, acumular CANDIDATO, e os dois 403 (persona EMPRESA; conta empresa tentando ativar outra persona). `/cadastro?perfil=hunter` recarregada no Chrome após os ajustes em `auth.ts`/`cadastro.vue` — sem erros de console. **Não testado**: cadastro por e-mail completo criando uma conta nova de verdade (exigiria digitar senha num formulário — fora da política) nem o fluxo OAuth completo do `auth/callback.vue`; a lógica de `register()`/`activatePersona()` foi conferida por revisão de código + `tsc` limpo.
- **Marketplace/fee (B4)**: `types/vaga.ts` — `Vaga` ganhou `feePercent`/`feeAmount`/`maxHunters`/`exclusivityDays`/`hunterContactPhone`; novos tipos `HunterInterestVaga`/`HunterInterest`. `VagaEditor.vue` tem os 4 campos de fee/cap/exclusividade (com computeds string-proxy `feePercentStr`/`maxHuntersStr`/`exclusivityDaysStr` — **`UiInput` não aceita `v-model.number`** porque seu `modelValue` é tipado `string`, então o modificador `.number` do Vue não converte; usar um `get/set` computed entre string↔number em vez disso) e validação client-side (fee obrigatório se `allowHunters`). `pages/vaga/[slug].vue` tem o drawer de termos ("Quero esta vaga" → aceitar termos → `POST /vagas/:id/hunter-interest`) com status (ACCEPTED/PENDING/REJECTED). Nova página `pages/app/hunter/marketplace.vue` (T-H07, nav em `useHunterWorkspace.ts`) lista `GET /vagas/radar?allowHunters=true` com filtros q/segmento/feeMin/order; **nota de tipo**: `order` é `ref<string|null>` (não `ref<'fee'|'recent'>`) porque `UiSelect.modelValue` é `string|null` — cast só no ponto de uso da API. `VagaCard.vue` mostra pill de fee (`feeLabel`). `CandidateDrawer.vue` mostra badge "Indicado por hunter" e mascara e-mail/telefone (`contactMasked`) com placeholder quando a candidatura é `source==='HUNTER'` e ainda não chegou na etapa configurada.
- **B4 validado E2E completo (2026-07-06)**: fluxo completo navegado no Chrome contra os servers reais — editor de vaga com fee/cap/exclusividade, marketplace `/app/hunter/marketplace`, drawer de termos em `/vaga/[slug]`, e `CandidateDrawer` com badge+masking no pipeline. Vaga de teste `desenvolvedora-full-stack-pleno` (conta `testeia@getnada.com`) restaurada ao estado original após os testes. Confirmado visualmente: erro de "fee obrigatório" chega como toast legível; pill de fee aparece no card/marketplace; checkbox de termos bloqueia o envio até marcado; mascaramento de contato muda exatamente no limiar de etapa esperado (3ª coluna). **Dívida**: cap de `maxHunters` (409 ao exceder) não testado ponta a ponta por UI real — só via proxy de validação (`maxHunters=0` rejeitado); precisaria de uma 2ª conta hunter para gerar o cenário genuíno.
- **Perfil/diretório de hunter (B5)**: novo `types/hunter.ts` (`HunterVerificationStatus`, `HunterMetrics`, `HunterDirectoryResponse` — atenção: shape `{items,total,page,limit}`, **não** o `PaginatedResult` comum usado em outras listagens — `HunterDirectoryCard`, `HunterPublicProfile`, `VerificationDocument`, `HunterVerificationRequest`, `VERIFICATION_STATUS_LABEL`/`VERIFICATION_STATUS_VARIANT`). `stores/auth.ts` → `User` ganhou `hunterSpecialties`/`hunterYearsExperience`/`verificationStatus`/`verificationRejectionReason`/`verificationDocs`/`verificationLinkedinUrl`. Páginas: `pages/hunters.vue` (T07, diretório público, filtros specialty/city/verifiedOnly/order), `pages/hunter/[username].vue` (T08, perfil público com grid de métricas).
- **Verificação de hunter (B8)**: `pages/app/hunter/perfil.vue` (T-H11) — self-edit dos campos públicos (`PATCH /profile`) + painel de verificação (upload de PDF via `POST /profile/me/verification/documents` com `FormData`, depois `POST /profile/me/verification/submit`). **Gotcha de reatividade corrigido**: `auth.user` só é populado pelo `fetchMe()` assíncrono do `onMounted` do layout pai (`app.vue`) — inicializar os refs locais diretamente de `auth.user?.campo` no setup captura valores vazios/stale em qualquer navegação fresca (e um "Salvar perfil" sem re-digitar tudo apagaria os dados reais). Padrão usado: refs começam vazios, um `watch(() => auth.user, cb, {immediate:true})` guardado por uma flag (`profileInitialized`) popula os campos uma única vez quando `auth.user` chega, sem nunca sobrescrever edições em andamento depois. Atenção à ordem de declaração: um ref referenciado dentro do callback `immediate` precisa estar declarado **antes** do `watch` (senão erro "used before declaration"). Gates de marketplace: `pages/app/hunter/marketplace.vue` (overlay + CTA quando não verificado), `pages/app/hunter/candidatos.vue` (banner âmbar), `pages/vaga/[slug].vue` (`toast.warning` com ação "Verificar perfil" no clique de "Quero esta vaga"), `components/hunter/SubmitCandidateModal.vue` (trata `err.code==='HUNTER_NOT_VERIFIED'` com mensagem própria) — todos checam `auth.user?.verificationStatus === 'APPROVED'`. Admin: `middleware/admin.ts` (novo — checa token + `auth.user.role==='ADMIN'`), `composables/useAdminWorkspace.ts` (novo, nav "Verificações de hunter"), `pages/app/admin/verificacoes.vue` (A2 mínimo: lista `PENDING`, aprovar 1-clique, recusar com modal + motivo obrigatório).
- **B5+B8 validado E2E completo (2026-07-06)**: ciclo completo no Chrome — diretório → perfil público → self-edit → upload PDF (`file_upload` no `<input type=file>` oculto, obtido via `read_page filter:'all'`, nunca clicar no botão visível que abre o picker nativo) → submit → fila admin recusa (motivo obrigatório) → hunter reenvia → admin aprova → selo "✓ Verificado" confirmado no diretório e perfil, marketplace/candidatos desbloqueados. **Bug de reatividade encontrado e corrigido nesta rodada** (ver acima, gotcha do `auth.user` assíncrono) — sem o fix, o hunter veria seus próprios dados de perfil somem a cada reload, risco real de perda de dados ao salvar por engano. Os outros 2 bugs desta rodada foram no backend (parsing de boolean em query string, `select` faltando `personas`) — ver `../vitrinepro-bakend/CLAUDE.md` §Ambiente.
- **Front de B10 (avaliações) + B12 (KPIs), 2026-07-07**: sem construir os workspaces Empresa/Consultoria completos (Fase 3/4 ainda não existem) — escopo desta rodada foi só trocar mocks/placeholders por dados reais dos endpoints novos de `/stats/*` e `/admin/stats` (backend, ver `../vitrinepro-bakend/CLAUDE.md` §B10/§B12):
  - `types/hunter.ts`: `HunterMetrics` ganhou `totalReviews` (junto do já existente `avaliacaoMedia`, agora real em vez de proxy).
  - `pages/hunter/[username].vue` e `pages/hunters.vue`: card/linha de "Avaliação média" agora mostra `{{ avaliacaoMedia }} ★ ({{ totalReviews }} avaliações)` em vez do número bruto sem contexto.
  - `pages/app/hunter/index.vue` (T-H02, "Minha mesa"): os 4 KPIs do topo (antes hardcoded `'0'`/`'R$ 0'`) agora vêm de `GET /stats/hunter` via `useAsyncData` + `UiKpiCard :loading`. Manteve o padrão já usado ali para `/vagas/me/usage` (slots).
  - `pages/app/empresa/index.vue`: placeholder "workspace em construção" ganhou uma faixa de 4 KPIs reais (`GET /stats/empresa`) acima do `UiEmptyState` — **não é o workspace Empresa completo**, só visibilidade dos números; CRUD de vagas/pipeline pra empresa continua não existindo (Fase 3).
  - `pages/app/admin/index.vue` (novo, A1 "Visão Geral" do §ADMIN): primeira tela real do workspace Admin além da fila de verificações — 6 KPIs + 3 "filas" (verificações/disputas/cupons) de `GET /admin/stats`. `composables/useAdminWorkspace.ts` ganhou o item de nav "Visão Geral" (agora primeiro da lista).
  - **Validado E2E no Chrome contra o backend real** com as 3 contas de sempre (`testeia`, `andresempresa`, `admin`): `/app/hunter` mostrou R$ 8.470/3/5/0 batendo exato com a API; `/hunter/andreshernandez9244` e `/hunters` mostraram "4 ★ (2)"; `/app/empresa` mostrou 1/2/0/2; `/app/admin` mostrou os 6 KPIs + filas zeradas. **Gotcha de sessão**: trocar de conta no Chrome via `/login` não desloga a anterior automaticamente — o cookie antigo (`vp_token`) permanece até o novo POST `/auth/login` suceder; se o clique no botão "Entrar" não registrar (coordenadas desatualizadas depois de uma navegação), a página parece "não logar" mas na real continua com a sessão anterior — sempre conferir `document.cookie` ou o nome exibido no header antes de assumir falha de login.
- **Workspace Empresa completo (Fase 3, 2026-07-11)**: `composables/useEmpresaWorkspace.ts` (novo, mesmo padrão do hunter) + 6 páginas novas em `pages/app/empresa/` (`vagas/index+nova+[id]/index+[id]/editar`, `candidatos.vue`, `hunters.vue`, `pagina.vue`, `config.vue`) + `components/empresa/ReviewHunterModal.vue`. `components/VagaEditor.vue` ganhou prop `basePath` (default `/app/hunter/vagas`) — o único lugar hardcoded pro hunter era o redirect pós-publicar (`irPipeline()`); agora `<VagaEditor base-path="/app/empresa/vagas" .../>` é só isso, zero duplicação de componente entre os dois workspaces. `types/vaga.ts` → `Vaga` ganhou `hunterInterestsAcceptedCount`/`hunterInterestsPendingCount` (só presentes quando `allowHunters=true`, novos no backend). `stores/auth.ts` → `User` ganhou os campos que já existiam no backend mas nunca tinham sido tipados no front (`bio`/`location`/`phone`/`website`/`profession`/`socialLinks`/`bannerUrl`/`companyIndustry`/`companyLogoUrl`/`hunterContactRevealStageOrder`) — sem eles, `pages/app/empresa/pagina.vue` e `config.vue` não compilariam com segurança de tipos. Candidatos cross-vaga (`candidatos.vue`) consome o endpoint agregado novo `GET /applications/me-as-owner` (ver `../vitrinepro-bakend/CLAUDE.md`); export CSV baixado via `$fetch(..., {responseType:'blob'})` do mesmo jeito que o `baixarPdf` do `CandidateDrawer.vue`. Aba "Meus hunters" de `hunters.vue` **não tem endpoint dedicado** — agrega client-side chamando `GET /vagas/:id/hunter-interests` pra cada vaga própria com `allowHunters=true` (N+1 aceitável, nº de vagas por empresa é baixo); se isso crescer, vale um endpoint agregado como o de candidatos.
- **Gotcha novo de Chrome MCP: `computer.type` não dispara sempre o evento `input` de um `<textarea>` grande.** Achado ao publicar uma vaga de teste como empresa: digitei uma descrição de 168 caracteres, a tela mostrava o texto e o contador "168/100 mínimo" corretamente, mas o clique em "Publicar vaga" continuava disparando o toast de erro "descrição precisa ter 100 caracteres" — ou seja, o `ref` do Vue (`description.value`) não tinha sido atualizado apesar do DOM mostrar o texto certo. Confirmado via `document.querySelector('textarea').value` (retornou o texto certo) vs. o comportamento da validação (indicando que o `v-model` nunca recebeu o evento `input`). Contorno que funcionou: `javascript_tool` disparando manualmente `element.dispatchEvent(new Event('input', {bubbles:true}))` depois do `type` — nesse ponto a validação passou a bater com o texto visível. Mesma família do bug já documentado de campos de login ficarem vazios; regra geral: **se uma validação client-side parecer errada apesar do texto estar visivelmente correto na tela, suspeitar do evento `input` primeiro, não do código de validação.**
- **Workspace Consultoria completo (Fase 4, 2026-07-11)**: `composables/useConsultoriaWorkspace.ts` (novo) + 8 páginas em `pages/app/consultoria/` (`index`, `vagas/index+nova+[id]/index+[id]/editar`, `pipeline`, `clientes/index+[id]`, `membros`, `faturamento`, `config`) + `types/team.ts` (novo — `Team`/`AccessibleTeam`/`TeamMember`/`Company`/`TeamPipelineApplication`/`TeamPlacement`/`ConsultoriaDashboardStats`/`ConsultoriaGanhosStats`). Entry point em `layouts/app.vue`: `accessibleTeams` (via `GET /me/team/accessible`) + botão "Consultoria" no dropdown do avatar quando não-vazio; `goToConsultoria()` ativa o contexto antes de navegar (padrão RN 00 §3.9). **Rotas flat** (`/app/consultoria/...`, não `/app/consultoria/[id]/...` do design-spec) — nenhum endpoint do backend consome `teamId` via parâmetro de rota, tudo resolve server-side por `TeamContextHelper.getTeamContext(user)`; ver comentário completo no topo de `useConsultoriaWorkspace.ts`. `VagaEditor.vue` ganhou prop `showClientSelector` (seletor de `Company` + badge "Publicando como {Time}" no header), reaproveitado em `consultoria/vagas/nova.vue` e `[id]/editar.vue` do mesmo jeito que `basePath` foi reaproveitado pra Empresa. **Resolução de papel do ator no front** (não é persistido em `auth.user`): páginas que precisam saber se o usuário é OWNER/MANAGER/RECRUITER buscam `GET /me/team` e comparam `team.ownerId`/`team.members` contra `auth.user.id` — mesmo padrão usado em `membros/index.vue` (`myRole`/`isOwner`/`canInvite`) e replicado em `clientes/index.vue` (`canManageClients`, adicionado depois de achar o gap na validação — ver abaixo). **Bug de nav corrigido nesta rodada, afeta TODOS os workspaces**: `layouts/app.vue` marcava "Início" como ativo em qualquer subpágina do mesmo workspace (`route.path.startsWith(item.to)` — o path de Início, ex. `/app/consultoria`, é prefixo de `/app/consultoria/clientes` etc.); trocado por `activeNavTo` (computed que escolhe o item de `to` mais longo que bate com a rota atual, em vez de checar cada item isoladamente).
- **Consultoria validado E2E completo (2026-07-11)**, contas "Time de B15" (ver `qa-test-accounts.json`, teamId `c9d7a6ba-03db-46cf-be23-f38504e0b3c9`): como **OWNER** (`b15owner...`) — Início com KPIs reais, criação de cliente "Acme Tecnologia" (+ atribuir recrutador via multi-select, +logo/setor/site), criação de vaga com cliente vinculado (badge "Publicando como Time de B15" + pill "Cliente" no pipeline da vaga), listagem em Vagas do Time com coluna Cliente/Responsável (reatribuição inline testada), Pipeline Geral com filtros, Membros (troca de papel MANAGER↔RECRUITER testada e revertida pra não afetar o caso negativo do B15), Faturamento & Ganhos, Configurações (perfil salvo com CNPJ/bio, tab Pipeline padrão). Como **MANAGER** (`b15manager...`): confirma acesso de escrita a Clientes e botão "Convidar membro", mas sem select de papel/"Remover" em Membros (só OWNER). Como **RECRUITER** (`b15recruiter...`, atribuído ao cliente Acme): Início/Vagas do Time/Pipeline Geral funcionam normalmente; Clientes mostra só o cliente atribuído. Como **RECRUITER2** (`b15recruiter2...`, caso negativo, não atribuído a nenhum cliente): Clientes vazio com empty-state "Você ainda não foi atribuído a nenhum cliente" — confirma RN-COMP-03 (escopo por atribuição, não só por papel). **Bug de UX encontrado e corrigido durante a validação** (não fazia parte do plano original, achado ao logar como RECRUITER): `clientes/index.vue` mostrava os botões "Novo cliente"/"⋯" (Editar/Atribuir/Excluir) pra RECRUITER mesmo o backend já bloqueando com 403 ("Recrutadores têm acesso somente leitura às empresas.") — a UI só não escondia os controles. Corrigido com o mesmo padrão de resolução de papel de `membros/index.vue` (`canManageClients`), e o empty-state também ganhou a variante certa pro caso RECRUITER-sem-atribuição (spec "Estados/empties chave" do design-spec 04). **Gotcha de Chrome MCP nesta rodada**: depois de editar `layouts/app.vue` (componente global, todo workspace o importa), o HMR do Nuxt às vezes trigger um reload completo da página que deixa a tab num estado "busy" por vários segundos — `computer.screenshot`/`get_page_text`/`read_page` retornam timeout ("Script injection timed out"/"Page still loading") mesmo a página estando ok visualmente depois. Abrir uma tab nova (`tabs_create_mcp`) e navegar de novo resolve mais rápido que insistir esperando na tab travada. Também aconteceu do dropdown de um `UiSelect` renderizar só 1 das N opções no screenshot (mas a árvore de acessibilidade via `read_page` mostrava todas corretas) — usar `computer left_click` com `ref` do `find`/`read_page` em vez de coordenada resolve, é só um artefato visual do screenshot/timing, não um bug real do componente.
- **Front do fluxo Placement P1-P4 completo (2026-07-11)**: `types/placement.ts` (novo — `Placement`/`PlacementSummary`/`PlacementTimelineResponse`, reaproveita `PLACEMENT_STATUS_LABEL` de `types/team.ts` em vez de duplicar). `components/MarkHiredModal.vue` (**P1**, `POST /applications/:id/placement`) — bloco de fee só aparece com `source==='HUNTER'`; mostra fee total real (mesma fórmula do backend, `feePercent`% do salário ou `feeAmount` flat) mas rotula o split 75%/25% como **estimativa**, porque o split real de uma empresa pode ter sido renegociado (B22, `placementPlatformSharePercent`) e só o servidor sabe o valor exato aplicado. `components/PlacementTimeline.vue` (**P3**, `GET /placements/:id/timeline`) — `UiStepper` + banner de exceção por status (DISPUTED/GUARANTEE_BROKEN/REPLACED/CANCELLED). `components/ReportDepartureModal.vue` (**P4**, `POST /placements/:id/departure`) — não oferece estorno proporcional (backend só implementa reposição gratuita via `replacesPlacementId`, ver B9). `CandidateDrawer.vue` ganhou seção "Contratação" com **atualização otimista via mutação direta do prop** (`props.application.placement = {...}` dentro do handler `onHired`/`onDeparted`) — funciona porque o prop é passado por referência do array reativo do pai, mesmo padrão que já existia informalmente em outros pontos do drawer, só que agora documentado explicitamente; evita esperar um refetch pra UI refletir a mudança. `pages/app/hunter/candidatos.vue` ganhou **P2** inline (Confirmar/Contestar com textarea) e `pages/app/hunter/ganhos.vue` (novo, **T-H09**) tem KPIs (`GET /stats/hunter/ganhos`) + lista de placements (`GET /me/placements/hunter`) com P2/P3 inline.
  - **Bug de CSS achado e corrigido: `UiStepper.vue` (componente compartilhado)** — o `.stepper__connector` (linha entre os círculos) tinha `position:absolute` com `z-index:0`. Regra de empilhamento CSS 2.1 Apêndice E: um elemento posicionado com `z-index:0`/`auto` entra no **mesmo nível de empilhamento** dos descendentes posicionados do seu container e pinta **depois** dos irmãos não-posicionados no mesmo nível do DOM — nesse caso, depois do `.stepper__text` (label/descrição), cobrindo o texto com um risco horizontal quando a descrição era curta (visível especificamente nos banners de exceção do `PlacementTimeline`, com labels mais curtos que o normal). Corrigido pra `z-index:-1` (garante que a linha pinte atrás do texto, não `0`/`auto`). **Regra geral pra próxima vez**: se um elemento absolute decorativo (linha, divisor, sombra) parecer "por cima" de um texto irmão que deveria estar na frente, suspeitar de `z-index:0` antes de qualquer outra coisa — `0` não é "neutro", ele cria um contexto de empilhamento.
  - **Bug de staleness achado e corrigido: `ganhos.vue`** — `confirmar()`/`enviarContestacao()` só chamavam o `refresh()` do `useAsyncData` da lista de placements; o `useAsyncData` separado dos KPIs (`hunter-ganhos-stats`) nunca era re-executado, deixando os KPIs do topo desatualizados até um reload manual da página mesmo com a ação tendo sucedido (toast + badge do item mudavam corretamente). **Regra geral**: página com 2+ `useAsyncData` independentes que uma mesma ação deveria atualizar precisa extrair `refresh` de cada um e chamar todos (`Promise.all`), nunca assumir que só um deles é o "certo".
  - **Validado E2E completo no Chrome contra o backend real** (conta `testeia@getnada.com`, vaga `74398f4d-8ed9-42e1-9d66-a2107d3bee10`): P1→P2 Confirmar→P3→P4 testado com o candidato "Consent Dominio" (GUARANTEE_BROKEN final); P1→P2 **Contestar** testado separadamente com "Candidato Email Teste" em `/app/hunter/candidatos` (badge "Em disputa" + banner "Contestado" com motivo). Notificação `PLACEMENT_HIRED` clicada e confirmada indo pro link corrigido `/app/hunter/ganhos` sem 404 (ver nota de F5/B13 acima sobre os 5 links quebrados). **Não exercitado nesta rodada** (aceito como gap, lógica de backend não tocada): caminho de contratação direta sem hunter (auto-`CONFIRMED`, já validado na sessão original do B9).
- **Painel Admin completo A1-A6+F13 (Fase 5, 2026-07-12)**: shell próprio — `composables/useAdminWorkspace.ts` reescrito com a nav completa (Visão Geral·Verificações·Disputas·Placements·Cupons·Usuários·Vagas·Empresas); `layouts/app.vue` ganhou `.shell__admin-strip` (faixa vermelha sticky "ADMIN" acima da topbar, `v-if="route.path.startsWith('/app/admin')"`). Páginas novas: `pages/app/admin/disputas.vue` (A3, lista `?status=DISPUTED` + modal resolver CONFIRM/CANCEL), `placements.vue` (A4, tabela global + ações "Forçar fee"/"Marcar estorno" com modal de motivo), `cupons.vue` (A5, `UiTabs` resgates/campanhas + modal criar/editar campanha), `usuarios.vue` (A6, busca + drawer com 4 seções: Plano/Papel de admin/Login-as/Excluir-anonimizar — cada uma com seu próprio motivo+botão, só a seção de LGPD usa reveal condicional `confirmingDelete` em vez de textarea sempre visível), `vagas.vue` (A6, busca+filtro+despublicar), `empresas.vue` (F13, tabela + modal editar split com histórico visível). Nova página pública `pages/login-as.vue` (`layout:false`) — lê `?token=`, chama `auth.loginWithToken(token)`, navega pra `/app`. `useApi().del` ganhou parâmetro `body?` opcional (precisava mandar `{reason}` no `DELETE /admin/users/:id`).
- **Gotcha documentado no código e confirmado na prática: cookie de sessão não é isolado por aba.** `vp_token` (via `useCookie`) é compartilhado por todo o navegador, não por aba — abrir o link de login-as (gerado em `usuarios.vue`, formato `/login-as?token=...`) numa 2ª aba do MESMO navegador **troca a sessão do admin na 1ª aba também**, silenciosamente (sem erro visível até a próxima ação). Foi reproduzido de propósito durante a validação: gerei o token, abri numa 2ª aba, voltei pra 1ª aba e tentei salvar uma troca de plano como admin → 403 "Acesso restrito a administradores" (a 1ª aba já não era mais o admin, virou o usuário impersonado). Por isso o design é **gerar um link pra copiar** (não navegar automaticamente na mesma aba) com instrução explícita na UI pra abrir numa janela anônima ou outro navegador. Recuperação: logout + login de novo como admin.
- **"Estorno" (A4 force-refund) não é um reembolso financeiro real** — decisão de honestidade de produto, não limitação técnica esquecida: como não existe gateway de pagamento (B11), a ação só cancela o placement no sistema (pra parar o relógio de fee/garantia) e loga o motivo pro financeiro agir por fora. O modal do frontend mostra esse aviso explicitamente (não é letra miúda).
- **Bug de segurança corrigido de passagem** (mesma classe já vista em B19/F7/B24): `CouponsService.listPendingRedemptions()` (backend) vazava `User` completo incl. hash de senha via `relations` aninhadas — ver `../vitrinepro-bakend/CLAUDE.md` §Admin A3-A6.
- **Validado E2E completo (2026-07-12)** contra o backend real, contas `admin@gmail.com` e `Bruno Candidato` (`b9candidate...@getnada.com`, ver `qa-test-accounts.json`) e uma conta descartável consumida (`b13verifteste@getnada.com`, anonimizada de propósito no final): A3 resolver disputa, A4 force-release-fee + force-refund, A5 criar/editar/toggle cupom de campanha, A6 Usuários (login-as completo incl. o cookie-gotcha acima, troca de plano Free→Recruiter, promote→demote admin ida e volta, excluir/anonimizar LGPD), A6 Vagas (despublicar), Empresas/F13 (editar split + ver histórico). Nenhum bug de produto restante.
- **Workspace Candidato completo (Fase 3, 2026-07-12/13)**: `composables/useCandidatoWorkspace.ts` (novo) + páginas em `pages/app/candidato/` (`index`, `radar`, `candidaturas`, `salvas`, `perfil/index`, `portfolio/index+novo+[id]`, `curriculos`, `formacao`) + `components/candidato/` (`OnboardingWizard.vue`, `PerfilSubnav.vue`, `PortfolioEditor.vue`, `RichTextEditor.vue`, `ConsentModal.vue`). Primeiro workspace construído do zero nesta sessão — só existia o placeholder "em construção" do F2. Reusa os padrões já estabelecidos nos outros workspaces: `KpiCard`/`VagaCard`/`ApplyModal`, o padrão `profileInitialized`+`watch(immediate:true)` do B8 pra popular refs de formulário só quando `auth.user` chega, `watchDebounced` do VueUse pra autosave (perfil, portfólio). **3 bugs reais encontrados e corrigidos, todos runtime-only** (invisíveis por leitura de código): (1) **ApplyModal com prefill em branco** em `radar.vue`/`salvas.vue` — modal atrás de `v-if` na mesma variável que também controlava `open:true`, montava já com `open=true`, e o `watch(() => props.open)` interno (sem `{immediate:true}`) nunca disparava porque Vue só chama watchers não-immediate em mudanças **pós-montagem**; corrigido removendo o `v-if` (modal sempre montado com `open:false`, props dependentes de vaga com `?? ''`/`?? null`) — **regra geral nova**: nunca gatear o mount de um modal pela mesma variável que também controla seu `open`, senão qualquer watcher não-immediate dentro dele nasce surdo pra primeira mudança. (2) **datas de Formação um mês/ano atrasadas** — `new Date("2018-02-01")` é interpretado como UTC meia-noite; `toLocaleDateString()` formata no fuso **local** do navegador, que em fusos atrás de UTC (America/Sao_Paulo, UTC-3) volta a data um dia (às vezes um mês) — corrigido extraindo ano/mês direto da string via `.split('-')`, nunca passando por `Date` quando só se quer re-exibir os mesmos componentes. (3) **vazamento de segurança em `GET /portfolio`** (achado proativamente construindo T-C07, não por falha de teste) — ver `../vitrinepro-bakend/CLAUDE.md` §Candidato. **Decisões deliberadas documentadas em comentário no código**: bio do candidato ficou como texto plano, não HTML (evita quebrar `pages/perfil/[username].vue`, que já renderiza `bio` via interpolação escapada); editor de portfólio usa um `RichTextEditor.vue` custom (`contentEditable`+`document.execCommand`) em vez de adicionar Tiptap/Quill, pra não exigir `npm install`+restart no meio da sessão; "Padrão" de CV tem exclusividade imposta **no frontend** (PATCH em paralelo em todos os CVs), já que o backend não impõe unicidade de `isActive`. **Validado E2E completo no Chrome contra o backend real**: onboarding (4 passos), Início (KPIs+completude+preview do Radar+atividade), Radar desktop (filtros+grid+saved-filters) e mobile (swipe deck simulado via override de CSS, já que `resize_window` não muda o `window.innerWidth` real — ver gotcha já documentado), Minhas Candidaturas (tabela+drawer+retirar), Vagas Salvas (tabs Ativas/Encerradas+banner deadline+remover com desfazer), Meu Perfil (Dados com autosave, Visibilidade com toggle+copy-link), Portfólio (criar→editar→upload capa/galeria→tags→publicar→ver na página pública), Currículos (upload+padrão+baixar), Formação (add+editar, incl. o fix de timezone confirmado com "02/2018" correto após reload), e o Modal de Consentimento LGPD ponta a ponta (ver `../vitrinepro-bakend/CLAUDE.md` §Candidato pro detalhe da conta de teste usada e por quê a conta de sempre não servia).
- **Achados de ambiente/tooling nesta rodada, não são bugs do produto** (documentando pra não re-derivar):
  1. **Clique por coordenada (`computer left_click x,y`) pode não registrar mesmo mirando certo**, quando a resolução real do viewport do navegador (ex. `window.innerWidth/innerHeight`, checado via `javascript_tool`) diverge da resolução da screenshot usada pra escolher a coordenada — nesta sessão o viewport real era 1920×889 mas os cliques eram calculados sobre uma screenshot menor. Sintoma: o clique "funciona" (sem erro) mas o estado da página não muda (ex. clicar em "Anonimizar conta" não revelava o textarea). **Diagnóstico rápido**: depois de um clique suspeito, checar o estado via `javascript_tool` (ex. `document.querySelector(...).textContent` ou um `.click()` nativo direto no elemento) antes de assumir que é bug de app. **Contorno que funcionou**: usar `find` pra pegar a `ref` do elemento e clicar por `ref` (não coordenada) — e se mesmo assim não refletir, cair pra `element.click()` via `javascript_tool` (clique de DOM nativo, sempre confiável, bypassa qualquer problema de coordenada/escala).
  2. **`computer.screenshot`/`get_page_text` podem falhar com "Script injection timed out"/"Page still loading" por vários minutos** mesmo com a página carregada e interativa (`document.readyState === 'complete'`, JS simples executando normal) — não é a página que trava, é especificamente a captura de screenshot/extração de texto que fica instável, provavelmente por causa do dev-server Nuxt não-bundled (600+ requests individuais de módulo por carregamento de página, muito mais pesado que um build de produção). **Diagnóstico**: se `javascript_tool` com uma expressão trivial (`1+1`, `document.title`) responde rápido mas `screenshot`/`get_page_text` não, o problema é da ferramenta de captura, não do app — seguir a validação via `javascript_tool` (ler `textContent`, contar toasts, clicar via `.click()`) em vez de insistir no screenshot.
  3. **Um `fetch()` para o backend (porta 3000) via `javascript_tool` pode travar a chamada de `Runtime.evaluate` inteira por 45s+**, mesmo com `AbortSignal.timeout()` curto — sinal de que o processo do backend ficou genuinamente sem responder (não é só lentidão do fetch em si, porque nem o `catch`/timeout do abort retornou). Quando isso acontece, não adianta insistir tentando de novo pela mesma via — melhor confirmar com o Andres se o backend real ainda está de pé antes de continuar. **[2026-07-12/13] Atualização importante**: nem sempre é o backend de verdade — nesta sessão o mesmo sintoma (`Runtime.evaluate` travado 45s+) ocorreu duas vezes seguidas mesmo **depois** do Andres reiniciar os servidores, e os logs de query do backend (TypeORM) mostravam queries normais sendo processadas sem erro no meio do "travamento" — ou seja, o processo Nest estava respondendo, era a própria aba/tab do Chrome MCP que tinha ficado num estado "busy". **Diagnóstico**: se um `javascript_tool` com uma expressão trivial (`'ping'`) volta rápido logo depois do timeout, o backend está OK — abrir uma tab nova (`tabs_create_mcp`) e repetir a chamada ali resolve, sem precisar pedir pro Andres reiniciar nada.
  4. **Clique sintético via `computer left_click` (por coordenada OU por `ref`) pode simplesmente não disparar o handler `@click` do Vue, sem erro nenhum e sem indício visual** — achado validando o `ConsentModal.vue` (T-C09/N): cliquei em "Autorizar", o modal fechou normalmente (o `@close`/`navigateTo` do componente pai disparou), mas `read_network_requests` mostrou **zero** requisição pro `POST /decide` — ou seja, o clique nunca chegou a executar o `onclick` de verdade, só teve efeito colateral nenhum visível pra denunciar isso. **Diagnóstico**: depois de qualquer clique cujo efeito esperado seja uma chamada de rede, checar `read_network_requests` (não só a UI) antes de concluir que "não funcionou" ou "é bug de produto". **Contorno que resolveu**: `document.querySelectorAll('button').find(b=>b.textContent.trim()==='Autorizar').click()` via `javascript_tool` (clique de DOM nativo) — disparou o `POST /decide` (201) na hora. Mesma família dos gotchas já documentados de clique por coordenada/tipo não confiável; a lição nova aqui é que a ferramenta de clique pode "ter sucesso" sem gerar o evento real, então a prova de verdade é sempre o network log ou o estado do backend, nunca só "o clique não deu erro".
- **F4 (WorkspaceSwitcher) + Conta (dados/segurança+indicações) + F10 (verificar-email) + F11 (QR) + cmd+K + F14 (sitemap/robots), tudo em 2026-07-12**: rodada de fechamento pré-deploy-staging, escopo explicitamente reduzido (Assinatura/M1 e pré-launch ficam de fora, ver PLANO). `components/WorkspaceSwitcher.vue` (novo, F4), `components/CommandPalette.vue` (novo, cmd+K — Ctrl/Cmd+K global via `useState('cmdk-open')` compartilhado com o botão de busca da topbar), `pages/app/conta/index.vue` (novo), `pages/verificar-email/[token].vue` (novo, `layout:'auth'`), `server/routes/sitemap.xml.ts`+`robots.txt.ts` (novos, primeira vez que este repo tem um diretório `server/` — rotas Nitro, auto-import de `defineEventHandler`/`useRuntimeConfig`/`$fetch`/`setHeader`), `components/ShareProcessModal.vue` (QR via `api.qrserver.com`, sem lib nova, mesma decisão já tomada pro editor de portfólio/sitemap).
  - **Bug real de runtime achado e corrigido: `setHeader(event, ...)` (helper auto-importado do h3 pelo Nitro) lança `TypeError: Cannot read properties of undefined (reading 'set')`** dentro de `server/routes/*.ts` nesta versão do projeto — o stack trace aponta pro `h3.mjs` do `node_modules` **raiz** do projeto, não o `h3` interno vendorizado do `nitropack`; ou seja, o auto-import do Nitro está resolvendo pro pacote errado quando há duas cópias de `h3` na árvore de dependências (uma via nitropack, outra hoisted na raiz), com assinaturas de `event` incompatíveis entre versões. **Sintoma em produção**: página de erro genérica 500 do Nuxt, sem detalhe nenhum na tela — só apareceu o motivo real quando o Andres colou o stack trace do terminal. **Correção que funciona sempre, independente dessa ambiguidade de resolução**: usar a API crua do Node diretamente, `event.node.res.setHeader(name, value)`, em vez do helper `setHeader(event, name, value)` do h3. Aplicado nos dois arquivos (`sitemap.xml.ts`/`robots.txt.ts`) — **regra geral pra qualquer `server/routes/*.ts` novo neste repo**: preferir `event.node.res.setHeader(...)` a `setHeader(event, ...)` até essa duplicação de `h3` no `node_modules` ser resolvida (ex. `npm dedupe` ou pin de versão), porque o bug não aparece no `tsc`/lint, só em runtime real.
  - **401-vs-400 no novo `POST /auth/change-password`, pego ANTES de chegar no Chrome**: o interceptor global de erros em `useApi.ts` trata **qualquer** resposta 401 como "sessão expirada" e força logout+redirect pro `/login` (`if (apiError.status === 401 && token.value) { token.value=null; navigateTo('/login...') }`). Isso significa que qualquer endpoint autenticado novo que precise sinalizar "valor atual errado, mas você continua validamente logado" (ex. senha atual incorreta) **tem que usar 400, nunca 401**, senão desloga o usuário no meio de uma ação que nem era sobre autenticação. Backend implementado desde o início com `BadRequestException` pro caso de senha errada (ver `../vitrinepro-bakend/CLAUDE.md`); confirmado ao vivo no Chrome que senha errada mostra o erro inline sem deslogar.
  - **Gotcha novo de Chrome MCP: uma tab pode ficar permanentemente travada ("Script injection timed out"/"Page still loading" por 45s+, indefinidamente) depois de um clique que abre um modal com fetch assíncrono no `watch`** — reproduzido abrindo o modal "Compartilhar processo" (`ShareProcessModal.vue`) duas vezes seguidas na mesma tab: a partir do 2º clique em "Compartilhar processo", `screenshot`/`get_page_text`/mesmo `navigate` pra a mesma URL pararam de responder, e nem esperar 5-10s resolveu (diferente do gotcha já documentado de HMR, que se resolve sozinho em segundos). **Diferença do gotcha antigo**: aquele era transitório (a página real não travava, só a ferramenta de captura); este parece ser um estado real da tab que só se resolve abrindo uma **tab nova** (`tabs_create_mcp` + `navigate`) e refazendo o fluxo do zero ali — não adianta insistir na mesma tab nem re-navegar nela. Não foi encontrado bug de produto correspondente (o modal funcionou normalmente na tab nova); é catalogado aqui como gotcha de tooling, não do app.
  - **Validado E2E completo no Chrome contra o backend real (2026-07-12)**: F4 — troca real Hunter→Consultoria (conta `b15owner`), sidebar/conteúdo mudam e contexto persiste. cmd+K — Ctrl+K abre, busca real de vaga por texto (`GET /vagas/radar?q=`) retorna resultado certo, ações rápidas mudam por workspace. Conta — troca de senha (senha errada→400 sem logout; senha certa→sucesso, ver `qa-test-accounts.json` pra senha atualizada), reenviar confirmação (toast ok), aba Indicações (código+estado vazio). F10 — conta descartável nova (`f10verify...@getnada.com`, token pego do `devEmailVerificationToken` do response de `/auth/register`, já que caixas de e-mail reais são bloqueadas pro Chrome MCP): 1ª visita→sucesso, reload do mesmo link (consumido)→"Link inválido" (404 mapeado certo). F11 — QR gerado/ocultado num link real do `ShareProcessModal` (conta `testeia`, candidato "Diego AutoConfirm"), revogado depois sem quebrar layout. F14 — `/sitemap.xml` e `/robots.txt` retornando conteúdo correto depois do fix de `setHeader`. Nenhum bug de produto pendente desta rodada além do já corrigido acima.
- **B27 — 2FA (TOTP), 2026-07-18, validado E2E**: contrato e decisões de produto em `../vitrinepro-bakend/CLAUDE.md` §B27 (é lá que mora a regra crítica de segurança). Do lado do front:
  - `stores/auth.ts` — `login()` mudou de contrato: agora devolve `{twoFactorRequired:true, challengeToken}` **ou** `{twoFactorRequired:false, twoFactorSetupRequired}`, em vez de sempre setar o token. O `challengeToken` é mantido **só em memória do componente**, nunca em `api.token`/cookie (o backend o rejeita em rotas autenticadas de propósito). Novo `verifyTwoFactor(challengeToken, code)`.
  - `pages/login.vue` — virou 2 etapas com `step: 'credentials' | 'twoFactor'`. Trata 429 (throttle) com mensagem própria e distingue "código inválido" de "verificação expirada" (challenge token dura 5min) pelo texto do erro — sem isso o usuário fica tentando códigos novos contra um token morto. `twoFactorSetupRequired` (ADMIN sem 2FA) redireciona pra `/app/conta?2fa=required`.
  - `pages/app/conta/index.vue` — card "Verificação em duas etapas" na tab Dados de acesso, com 4 estados (inativa / configurando / mostrando códigos / ativa). Códigos de recuperação aparecem **uma única vez**, com Copiar/Baixar .txt (blob local — nunca voltam ao servidor) e botão "Concluir" bloqueado até o usuário marcar que os guardou.
  - **`components/ui/Input.vue` ganhou 3 props e um `defineExpose`** — mudança num componente compartilhado, vale saber por quê: o root do `UiInput` é a `div.field`, então `inputmode`/`maxlength`/`autofocus` passados de fora pousavam **na div e não faziam nada, silenciosamente** (fallthrough de atributos). Agora são props explícitas repassadas ao `<input>` real. E `ref` num componente devolve a instância, não o elemento — sem `defineExpose({focus})` não havia como focar o campo de fora. Preferi props explícitas a `inheritAttrs:false` + `v-bind="$attrs"` porque este último moveria `class`/`style` da div pro input e poderia quebrar usos existentes.
  - `pages/precos.vue` — linha "Verificação em duas etapas (2FA)" com ✓ nos 4 planos + item de FAQ respondendo que é gratuita. A lista do card do plano Gratuito vem do backend (`PLAN_FEATURES`).
  - **Bug real encontrado e corrigido dirigindo a UI (só apareceu aí, não no E2E de API)**: os formulários de "Gerar novos códigos" e "Desativar" **setavam `tfaError` mas nunca o renderizavam** — senha errada falhava em **silêncio absoluto** (o backend rejeitava certo, mas a tela não dizia nada). Adicionado `<p v-if="tfaError" class="conta__error">` nos dois. Corrigido junto: os dois formulários compartilham o mesmo `tfaError`, então um erro de "regenerar" reaparecia ao abrir "desativar" — os toggles agora limpam `tfaError` e fecham o outro formulário. **Regra geral**: ao reusar um único ref de erro entre formulários irmãos, limpar no toggle e conferir que TODO formulário que escreve nesse ref também o exibe.
  - **Validado E2E no navegador (2026-07-18)**, contas descartáveis anonimizadas ao final: ativação completa pela UI (segredo formatado em grupos de 4, `inputmode`/`maxlength` confirmados aplicados no input real, 10 códigos, guard do checkbox); login em 2 passos (código errado → erro inline **sem cookie de sessão criada**, correto → `/app/candidato` com cookie); **código de recuperação pela UI** (o toggle troca label/placeholder/`maxlength` 6→9/`inputmode`, login OK, toast "Restam 9"); botão **"Voltar"** (volta ao passo 1, limpa a senha, preserva o e-mail); **regenerar e desativar** (senha errada → erro visível após o fix, 2FA segue ativo); **estado ADMIN obrigatório** (conta promovida a ADMIN entra **sem bloqueio**, é redirecionada pra `/app/conta?2fa=required`, badge vermelho "Obrigatória" + aviso, configuração abre sozinha); `/precos` conferida; 390px sem overflow (técnica do iframe do F15).
  - **Limitação de tooling desta rodada**: `computer.screenshot` deu timeout em todas as tentativas, inclusive em tab nova (gotcha #2 já documentado — `javascript_tool` respondia em 23ms com `readyState:complete`, ou seja, a app estava perfeita e o problema era só a captura). **Não houve confirmação visual em nenhum momento** — a validação foi por DOM + auditoria de estilo computado (tokens resolvendo, alturas de botão, ausência de texto cortado, zero overflow em 1280px e 390px).
  - **⚠️ Lacunas de validação do front (não confundir com validado)** — a lista completa das 3 está em `../vitrinepro-bakend/CLAUDE.md` §B27. As que afetam esta camada: (1) **aparência visual nunca foi vista por ninguém**; (2) o ramo de **conta OAuth** dos formulários de desativar/regenerar (`v-if="!tfa.isOAuthAccount"` → pede código do app em vez de senha) **nunca renderizou**, porque todas as contas de teste eram locais.
- **OPS6 — Google Tag Manager com Consent Mode v2 (2026-07-18), validado E2E no navegador**: contêiner `GTM-TLTNFG8T`. Fecha a parte de instrumentação do OPS6, incluindo a exigência que o próprio gap trazia ("LGPD: banner de cookies já existe — ligar o consentimento ao script").
  - **⚠️ Modo de carga — mudou por decisão do Andres no mesmo dia.** Primeiro foi entregue no **modo estrito** (contêiner só depois do aceite; recusa total = zero requisição ao Google). O Andres pediu para carregar sem esperar consentimento — **default hoje: `gtmRequireConsent: false`, contêiner carrega sempre**. Trocar para `true` no `nuxt.config.ts` volta ao estrito.
    - **O Consent Mode v2 continua ativo nos dois modos** — é ele que faz a diferença: defaults **tudo negado** antes de qualquer tag, e `consent update` a cada decisão. Enquanto o usuário não aceita, tags que dependem de armazenamento operam cookieless. É o padrão que o próprio Google recomenda.
    - **Trade-off registrado** (não é esquecimento): carregando sempre, há requisição ao Google antes de qualquer aceite — a pessoa aparece nos logs do Google mesmo se depois recusar. Era exatamente o que o OPS6 original pedia para evitar. **Ganho prático que motivou a mudança**: o modo Visualizar do GTM / Tag Assistant não conecta se o contêiner nunca carrega, então era impossível configurar e depurar tags. Se a assessoria jurídica pedir o estrito, é uma variável de ambiente.
  - **Mapeamento granular**: análise → `analytics_storage`; marketing → `ad_storage`+`ad_user_data`+`ad_personalization`. Aceitar só análise **não** libera tag de anúncio (confirmado E2E).
  - **`useConsentimento()` (novo composable)**: o estado saiu do `CookieBanner.vue`. Motivo concreto — `useCookie` devolve um **ref novo a cada chamada**, então o banner gravaria o cookie e o plugin continuaria vendo o valor antigo até um reload. `useState` por cima do cookie resolve, e o plugin reage via `watch`.
  - **`<noscript>`** (`app.vue`, lido no SSR): segue a mesma regra do plugin — no modo padrão sai sempre; no modo estrito, só para quem já consentiu antes (quem está sem JS não consegue ver o banner, então rastreá-lo seria rastreio sem consentimento por definição). Validado por curl nos dois modos.
  - **Botão "Recusar" adicionado ao banner** — antes aceitar era 1 clique e recusar exigia 4 (Configurar → desmarcar 2 → Salvar). Padrão escuro que a LGPD (art. 8º §4º) e o EDPB tratam como consentimento viciado; era cosmético enquanto os toggles não faziam nada, virou material com o GTM ligado.
  - **Pageview de SPA — 3 tentativas erradas antes de acertar, todas medidas ao vivo** (vale ler antes de mexer): (1) `router.afterEach` dispara antes de a página montar → `page_title` saía com o título da página **anterior**; (2) nem `nextTick`+`requestAnimationFrame` nem um `MutationObserver` no `<title>` com teto de 400ms consertaram — em dev os módulos carregam um a um e o título atrasa mais que qualquer espera razoável; (3) `useRoute()` **fora de um componente devolve valor stale** (reportou `/` em duas navegações seguidas) — a fonte confiável é `router.currentRoute.value`. **Solução final**: disparar em `page:finish` (garante montagem), enviar só `page_path`+`page_location` e **deixar o título para a tag do GA4 dentro do GTM**, que o lê no momento em que dispara. Mandar título errado suja relatório em silêncio; melhor não mandar. **Dedupe por caminho, não flag de "primeira navegação"** — `page:finish` **não dispara na carga inicial**, então a flag comia a primeira navegação real (`/precos` nunca era contado).
  - **Config: direto no `nuxt.config.ts`, SEM variável de ambiente** (pedido explícito do Andres — chegou a existir `NUXT_PUBLIC_GTM_*` e foi removido). Três constantes em `runtimeConfig.public`: `gtmId: 'GTM-TLTNFG8T'`, `gtmEnabled: true` (false desliga tudo, inclusive o noscript) e `gtmRequireConsent: false` (true = modo estrito). **Mudar exige editar a linha e fazer deploy** — se um dia precisar variar por ambiente, é voltar a ler de `process.env` como as chaves de OAuth logo acima. **Consequência**: hoje o tráfego de dev **é contado** — filtrar por hostname dentro do painel do GTM é o caminho, já que não há mais env para desligar localmente.
  - **Política de cookies atualizada** (`pages/cookies.vue`): a frase "ainda não usamos cookies de publicidade" deixou de ser verdadeira com um contêiner que pode hospedar tags de anúncio; agora nomeia o Google Tag Manager e o Consent Mode, e explica que as categorias são independentes.
  - **Validado E2E nos dois modos.** *Estrito* (como foi entregue primeiro): sem decisão → nenhuma requisição ao Google e defaults negados; recusar tudo (pelo modal **e** pelo botão novo) → contêiner nunca baixado; `<noscript>` presente só com consentimento prévio. *Padrão atual* (carrega sempre): sem nenhuma decisão de cookies → `google_tag_manager['GTM-TLTNFG8T']` já inicializado com defaults **negados**; aceitar → update `granted`; recusar depois → update volta a `denied` com o contêiner já carregado; `<noscript>` sempre presente. Comuns aos dois: só análise → publicidade segue `denied`; 5 navegações SPA → exatamente 5 eventos na ordem certa, sem duplicar a home.
- **T-GUIA — Guias de uso por persona (2026-07-18), validado no navegador**: `/guias` (índice) + `/guias/[persona]` (candidato·hunter·empresa·consultoria). Conteúdo em **fonte única**: `shared/guias.ts`, consumido pela página Vue, pelo markdown e pelo `llms.txt` — mudar um passo atualiza os três.
  - **Componentes** `app/components/guias/`: `GuiaMockup.vue` (16 variantes de "screenshot" desenhadas em SVG + CSS) e `GuiaPasso.vue` (um passo, com revelação no scroll).
  - **Por que maquete SVG e não screenshot real**: captura envelhece no primeiro ajuste de UI e ninguém lembra de trocar; pesa; não se adapta a mobile nem a tema. As maquetes usam os mesmos tokens de `tokens.css`, então acompanham o design sozinhas. São abstratas de propósito — comunicam a forma da tela, não prometem retrato fiel.
  - **Regra de escrita (respeitada, medida ao vivo: 6–12 palavras por passo)**: `texto` é UMA linha. Contexto longo vai em `detalhe`, que só aparece no markdown e para leitor de tela — a guia visual não incha.
  - **🐛 Bug de routing real, custou a página inteira**: o arquivo `server/routes/guias/[persona].md.ts` (ideia original: servir `/guias/hunter.md`) foi registrado pelo Nitro como **`/guias/:persona`** — o sufixo `.md` é engolido pelo parser do parâmetro. Rota de servidor tem precedência sobre página do Nuxt, então `/guias/hunter` passou a devolver **markdown cru no lugar da guia visual**. Tentativa de contornar retornando `undefined` para "deixar passar" **não funciona**: a resposta sai `204` com corpo vazio, não cai no renderer do Nuxt. **Solução**: mover para `server/routes/llms/guias/[persona].md.ts` — sob `/llms/` não há página Vue, então a captura ampla é inofensiva (e `/llms/guias/hunter` e `.../hunter.md` respondem os dois). **Regra geral: nunca criar `server/routes/<x>/[param].*` quando existe uma página `pages/<x>/[param].vue` — a rota de servidor sempre vence.**
  - **🐛 2º bug real: `IntersectionObserver` deixava passos invisíveis para sempre.** O IO só dispara ao *cruzar* um threshold; num salto instantâneo (`Ctrl+End`, âncora, restauração de scroll ao recarregar) o elemento vai de "não intersecta (abaixo)" direto para "não intersecta (acima)" sem nenhum frame amostrado no meio — **nenhum callback dispara**. Reproduzido: 2 de 6 passos ficavam mortos após `scrollTo` até o fim; checar `boundingClientRect.top < 0` dentro do callback não resolve, porque o callback nem roda. **Solução**: listener de `scroll`/`resize` passivo com guarda de `requestAnimationFrame`, que revela quando `top < innerHeight - 60` (verdadeiro também para quem já passou) e se remove sozinho. Validado nos dois cenários: 6/6 com salto instantâneo e 6/6 com scroll gradual.
  - **Leitura por IA** (pedido explícito do Andres): `/llms.txt` (convenção llmstxt.org — o que o site é, glossário do produto e links), `/llms/guias/[persona].md` (markdown puro, `text/markdown`, CORS liberado), JSON-LD **`HowTo`** + **`FAQPage`** em cada guia, `<link rel="alternate" type="text/markdown">`, HTML semântico (`<article>`, `<ol>` de passos, hierarquia h1→h2→h3) e todos os SVG com `role="img"`+`aria-label`+`<title>`. Guias adicionadas ao `sitemap.xml`; `robots.txt` aponta o `llms.txt`.
  - **Acessibilidade**: toda animação morre em `prefers-reduced-motion: reduce` (6 regras confirmadas ao vivo — atenção: o browser normaliza `animation: none` para a forma longa `animation: auto ease 0s 1 normal none running none`, então um grep por `animation:\s*none` no `cssText` dá falso negativo). O estado "escondido" dos passos só é aplicado quando há JS **e** o usuário aceita movimento — sem isso a página nasceria em branco.
  - **Validado ao vivo (2026-07-18)**: as 4 guias + índice em 200, `/guias/inexistente` → 404 real (não página vazia), markdown com `content-type: text/markdown`, JSON-LD parseável com 6 passos e 4 FAQs, 6 maquetas com `aria-label`, zero overflow em 1280px **e** em 390px (iframe do F15), e no mobile a maquete vem sempre **depois** do texto (ler primeiro, ver depois). Nav e footer ganharam link para `/guias`.
- **F15a/F15b (2026-07-13), 2 bugs de mobile reportados pelo Andres com screenshot, corrigidos no mesmo dia** — ver PLANO §CABOS SOLTOS. (1) Painel do sino (`.shell__notif-menu`) tinha `width:340px`+`position:absolute;right:0` **relativo ao ícone**, não à tela — em viewport estreito nascia parcialmente fora do canvas, cortando texto. (2) Clicar em "Início" na sidebar mobile estando **já** em "Início" não fechava o drawer, porque o fechamento dependia só de `watch(() => route.fullPath)` e o Vue Router não dispara navegação pra rota já atual. **Fix de ambos em `layouts/app.vue`**: `@media max-width:768px` agora vira `.shell__notif-menu`/`.shell__user-menu` em `position:fixed` ancorado nas bordas da tela (`left/right:var(--sp-3)`, não mais relativo ao ícone); cada `.shell__nav-item` ganhou `@click="sidebarOpen=false"` direto (fecha no clique em si, não depende de a rota mudar).
  - **Gotcha novo de tooling, importante pra qualquer validação mobile futura**: `resize_window` do Chrome MCP **não muda o `window.innerWidth`/`outerWidth` reais** neste ambiente — testado (390×844 pedido, `window.innerWidth` continuou 918 e `outerWidth` 1920 depois da chamada). Sintoma: `@media (max-width:...)` nunca ativa, então qualquer teste "mobile" via `resize_window` sozinho é **falso-negativo silencioso** (a página renderiza no layout desktop, parece "não ter bug", mas não testou nada). **Contorno que funciona de verdade**: injetar um `<iframe>` same-origin com `width`/`height` fixos (via `javascript_tool`, `document.body.appendChild`) apontando pra mesma URL — um iframe tem seu próprio `window`, então `window.innerWidth` **dentro dele** reflete o tamanho real do elemento, ativando as media queries de verdade; cookies/sessão são compartilhados por ser mesma origem, então a página carrega já autenticada. Interagir com o conteúdo do iframe via `iframe.contentDocument.querySelector(...).click()` (JS direto, mais confiável que clique por coordenada num elemento aninhado); pra ver o resultado, `computer.zoom` na região exata do iframe (`region: [0,0,largura,altura]`) em vez de screenshot da página inteira. Remover o iframe (`.remove()`) ao terminar. Vale documentar de novo se aparecer um teste "mobile" futuro que passa por `resize_window` sem essa checagem — sem o iframe, a validação não teria pego nenhum dos dois bugs do F15.
- **F16 (2026-07-16), auditoria mobile completa (pedido explícito do Andres, ver PLANO)** — feita em 2 etapas: (1) grep sistemático de `width` fixo (68 arquivos) e `position:absolute/fixed` (36 arquivos) em todo o repo pra triagem estática rápida (descartando padrões já seguros, como dropdowns ancorados no próprio trigger e componentes que já tinham breakpoint mobile); (2) validação real por página com o contorno de iframe 390px do F15 (helper reutilizável `window.__checkPage(url)` — navega o iframe, espera `nextTick`, e devolve `{innerWidth, docScrollWidth, overflow}`; um `overflow>0` sinaliza bug real, daí um segundo helper varre `querySelectorAll('*')` + `getBoundingClientRect()` pra achar o elemento exato responsável). Cobertos: as 10 páginas do Admin, as páginas já testadas de Hunter/Empresa/Consultoria/Candidato (confirmado limpo, herdado de F13/F15), todas as páginas públicas de marketing, cmd+K, e uma amostra de modais. **5 bugs reais achados e corrigidos**:
  1. `pages/app/admin/index.vue` — `.overview__kpis` só tinha `@media(max-width:900px)` descendo pra 2 colunas; a 480px ainda estourava ~10px. Novo `@media(max-width:480px)` empilha em 1 coluna.
  2. `layouts/app.vue` (afeta os 5 workspaces) — topbar com ~8-23px de overflow (`.shell__breadcrumb` sem `min-width:0`/`text-overflow`, `.shell__burger`/`.shell__topbar-right` sem `flex-shrink:0`) + sidebar mobile sem jeito de fechar clicando fora. Adicionado backdrop (`.shell__sidebar-backdrop`, `position:fixed;inset:0`) que fecha o `sidebarOpen` no clique.
  3. `pages/index.vue` (Home) — dois problemas na hero section em ~375-390px: o toast decorativo "Placement confirmado" (`.toast-placement{position:absolute;right:-8px}`, pensado pra "vazar" um pouco no desktop) vazava de verdade pra fora do viewport quando o `.hero__mock` passa a ocupar 100% da largura (breakpoint de 900px já existente); e `.hero__ctas` (os 2 botões do hero) sem `flex-wrap`, forçando a página inteira a ganhar 20px de scroll horizontal porque os 2 botões não cabem lado a lado. Fix: `right:0` pro toast dentro do `@media(max-width:900px)` já existente, e `flex-wrap:wrap` na regra base de `.hero__ctas`.
  4. **`components/ui/Modal.vue` — bug estrutural, o mais importante desta rodada, porque `UiModal` é a base de praticamente todo modal do app** (`UpgradeModal`, `MarkHiredModal`, `ReportDepartureModal`, `ConsentModal`, `ReviewHunterModal`, diálogos de confirmação, etc.): `.modal__footer{display:flex;justify-content:flex-end;gap:...}` não tinha `flex-wrap`. Reproduzido ao vivo forçando o `UpgradeModal` (conta descartável `upgrademodal...@getnada.com`, ainda no limite FREE 1/1) num iframe de 390px: o footer tem 2 botões ("Ver todos os planos" + "Fazer upgrade — R$ 50/mês") que juntos não cabem em 390px, e por causa do `justify-content:flex-end` o **primeiro botão vazava pra fora da tela pela esquerda** (`getBoundingClientRect().left` negativo, ex. `-40px`), cortando o texto — só apareceu porque o overflow não mexe no `document.scrollWidth` da página (o vazamento é *dentro* do modal, que tem seu próprio clipping), então só foi pego inspecionando o elemento em si, não só o overflow da página. **Fix**: `flex-wrap:wrap` no `.modal__footer` + novo `@media(max-width:480px)` empilhando os botões em coluna (`flex-direction:column-reverse`, ação primária — último botão no DOM — em cima, `width:100%` em cada um). Validado ao vivo: o mesmo diálogo de confirmação "Publicar vaga?" (2 botões "Cancelar"/"Publicar") também estava sujeito ao mesmo bug e ficou correto depois do fix, sem precisar tocar o componente do diálogo. Esse fix central cobre automaticamente qualquer outro modal de 2 botões no app sem precisar validar cada um individualmente.
  - **Confirmado limpo, sem bug** (documentado pra não reabrir a mesma investigação à toa): tabelas (`UiTable.vue` já embrulha em `.table-wrap{overflow-x:auto}`; as 8 tabelas fora desse componente compartilhado têm o mesmo padrão próprio, `.xxx__table-wrap{overflow-x:auto}` — nenhuma tabela sem scroll no app inteiro); `Select`/`MultiSelect` (`position:absolute;left:0;right:0` relativo ao próprio trigger, cresce/encolhe com o container — sem risco de overflow relativo ao viewport); `WorkspaceSwitcher` (já escondia o label em `max-width:768px`, preexistente); o carrossel de vagas em destaque na Home (`.destaque__trilho{overflow-x:auto}` intencional — o `getBoundingClientRect()` de um item dentro dele pode aparecer "fora" do viewport, mas é clipado pelo próprio scroll do carrossel, não afeta `document.scrollWidth`); `CurrencyInput` (só um label "R$" absoluto dentro do próprio input). `Datepicker` (popover `width:280px` era a única *suspeita* de risco levantada pela análise estática — testado ao vivo nos 2 usos reais do componente, `VagaEditor.vue` (sidebar) e `MarkHiredModal.vue` (`.mh__fields`) — ambos ficam em coluna única/full-width abaixo de 640-900px, então o popover `left:0` fica ancorado num trigger que já ocupa a largura toda, sem vazar).
- **F16b (2026-07-16), design do editor de Portfólio (candidato) fora do padrão + bug real de dados sumindo** — pedido do Andres: "criar um novo [projeto] não me parece adequado ao design system, está um pouco estranho", + revalidar todo o fluxo de portfólio buscando bugs. **Comparação com `VagaEditor.vue`** (a outra tela de "criar/editar entidade única" do app, usada como referência de padrão) expôs 3 divergências em `components/candidato/PortfolioEditor.vue`: reusava `<h1>Meu Perfil</h1>` + `CandidatoPerfilSubnav` (subnav do hub, do T-C06) numa página de edição de item — a subnav nem destacava nenhuma aba ali (comparação exata de rota falha num sub-path) e não faz sentido nesse nível; título do projeto era um `<input>` cru sem borda/label ("hero-style"), padrão que não existe em nenhum outro editor do app; ícones da galeria (mover/excluir arquivo) eram emoji cru (↑↓🗑📄) em vez de lucide, inconsistente até com o `RichTextEditor.vue` vizinho na mesma tela. **Fix**: `<h1>` dinâmico "Novo projeto"/"Editar projeto" + link "← Portfólio" (sem a subnav do hub), campo de título como `UiInput` normal, ícones trocados por `ArrowUp`/`ArrowDown`/`Trash2`/`FileText` (lucide) com o mesmo botão 28×28+hover do toolbar do editor de texto rico.
  - **Bug real de dados encontrado (mais grave que o de design), 100% reproduzível**: `pages/app/candidato/portfolio/index.vue` e `PortfolioEditor.vue` fazem `GET /portfolio?userId=<próprio id>` (o backend exige esse `userId` bater com o requisitante JWT pra liberar rascunhos — ver F7/`portfolio.service.ts`). `auth.user` (de onde vem o id) só é populado pelo `fetchMe()` do `layouts/app.vue`, chamado em `onMounted()` — que roda **depois** do setup da página. Numa navegação direta/hard-reload/aba nova pra qualquer uma dessas 2 páginas, `auth.user` ainda é `null` no momento da query (nenhum `watch` re-dispara depois), então ela cai sempre no fallback `Promise.resolve({data:[]})` — **o portfólio inteiro (rascunhos+publicados) sumia da tela**, parecendo perda de dados, com os itens intactos no banco (confirmados reaparecendo via navegação client-side por outra página, e via checagem direta do backend numa 2ª aba). Reproduzido de forma determinística 3 vezes seguidas, inclusive em abas novas sem qualquer estado prévio. **Fix**: `if (!auth.user) await auth.fetchMe()` antes da query nas 2 páginas. **Este é o mesmo padrão de bug de timing do F16 (`UiModal`/mobile) — algo populado só em `onMounted()` de um layout compartilhado não pode ser assumido disponível no `setup()` de uma página filha; vale grep por `auth.user` sem esse guard se aparecerem sintomas parecidos ("dados sumindo" em qualquer tela nova que dependa de `auth.user.id` logo de cara).**
  - Validado E2E completo em Chrome com `testeia@getnada.com` (persona candidato, conta sem portfólio prévio nesta rodada): criar projeto → salvar rascunho → upload de capa (JPEG) e arquivo de galeria (PNG, via `file_upload` da extensão Chrome — arquivos de teste sintéticos gerados na sessão, contornando a limitação de não poder acessar o picker nativo do SO) → criar tag inline (`@keyup.enter`, não `keydown` — atenção nesse detalhe se for reescrever um teste parecido) → publicar → conferido em `/portfolio/<slug>` (capa/tag/galeria renderizando) → despublicar/excluir pelo menu "⋯" da listagem, confirmado removido. **Gotcha de tooling desta rodada**: a tab do Chrome MCP travou (`Page.captureScreenshot timed out`) 4 vezes seguidas após cliques em botões dentro de modais (publicar, confirmar) — mesma classe de flakiness já documentada; contornado abrindo aba nova a cada vez. Numa dessas travas, o clique em "Publicar" (dentro do `UiConfirmDialog`) ficou com o botão preso em estado `loading` (disabled) indefinidamente NA TAB QUE TRAVOU, mas uma aba nova confirmou que o `PATCH` chegou e persistiu `status:PUBLISHED` no backend normalmente — ou seja, o travamento foi só da tab/ferramenta de automação, não do produto; ainda assim, vale notar como gap de robustez (não bug confirmado): não há timeout/retry no `save()`/`confirmPublish()` — se uma resposta real se perder por qualquer motivo de rede, o usuário fica preso num modal com os 2 botões desabilitados sem saída a não ser recarregar a página.
- **F16c (2026-07-16), menu "⋯" do card de Portfólio renderizava clipado/invisível** — reportado pelo Andres com screenshot logo depois do F16b: "los menu de las cosas aparecen y no se vem quedanb debajo", com suspeita de ser um padrão do projeto inteiro ("en todo el proyecto"). **Causa raiz**: `.portfolio__card{padding:0;overflow:hidden}` em `pages/app/candidato/portfolio/index.vue` só existia pra arredondar os cantos de cima do `.portfolio__cover` (a imagem de capa), mas o `.portfolio__menu` (dropdown `position:absolute`, filho de `.portfolio__body` que por sua vez é filho do card) é descendente desse mesmo container — `overflow:hidden` num ancestral corta visualmente qualquer parte de um descendente que ultrapasse sua caixa, **não importa o `z-index`** do descendente (`z-index:5` no `.portfolio__menu` não ajuda em nada aqui, o corte acontece antes do compositing de z-index entrar em jogo). **Fix**: removido `overflow:hidden` do `.portfolio__card`; o `border-radius` que ele existia pra dar (só nos cantos de cima, já que o card inteiro já é arredondado por baixo via `UiCard`) foi movido direto pro `.portfolio__cover` (`border-radius: var(--radius-card) var(--radius-card) 0 0`). Resultado: a capa continua com os cantos certos, e o dropdown agora renderiza por cima de tudo sem corte.
  - **Checagem do escopo "todo o projeto"**: grep por `overflow:\s*hidden` em `app/` retornou 10 arquivos. Os outros 9 (além do já corrigido) foram lidos um a um e nenhum tem o mesmo anti-padrão de "container corta seu próprio menu/dropdown": `layouts/app.vue` (`.shell__breadcrumb` — só truncamento de texto com ellipsis, o sino/menu de avatar já usam `position:fixed` fora desse container, ver F15a); `UpgradeModal.vue` (`.upgrade__bar` — clip de uma barra de progresso, sem filho absoluto); `CommandPalette.vue` (`.cmdk` — o próprio modal com scroll interno, e `.cmdk__item-label` — ellipsis); `WorkspaceSwitcher.vue` (`.wsw__label`/`.wsw__item span` — ellipsis; o `.wsw__menu` dropdown real é irmão, não filho, do elemento com overflow); `RichTextEditor.vue` (`.rte` — só arredonda os cantos da caixa com borda, toolbar/corpo não têm menu próprio); `consultoria/pipeline.vue` e `consultoria/index.vue` (`.kcard__vaga`/`.pipeline__stage`/`.slots__bar`/`.pipeline__track` — todos ellipsis ou barra de progresso); `ui/Avatar.vue` (clip circular da própria imagem/iniciais, sem filho absoluto); `assets/css/base.css` (`.visually-hidden` — utilitário de acessibilidade, não é layout de card). Conclusão: não era um bug generalizado do design system, era específico da estrutura do card de Portfólio.
  - Validado ao vivo no Chrome (`testeia@getnada.com`, `/app/candidato/portfolio`): reaberto o menu "⋯" do card "Projeto Teste E2E" — as 3 opções (Editar/Despublicar/Excluir) aparecem completas, sem corte, renderizando por cima da área abaixo do card.
- **F18 (2026-07-16), auditoria de affordance de clique (cards/linhas que deveriam navegar)** — reportado pelo Andres: clicar no card de um item de portfólio não levava a lugar nenhum; pedida varredura dos fluxos Candidato/Empresa/Hunter/Consultoria. **Padrão do app (agora explícito)**: item de lista que representa entidade navegável = clique no item INTEIRO navega (`UiCard clickable`+`@click`, `NuxtLink` envolvendo o card, ou `tr` com `cursor:pointer`+`@click`), e ações secundárias (menu "⋯", links externos) levam `@click.stop` pra não disparar a navegação do pai. **2 gaps corrigidos**: `pages/app/candidato/portfolio/index.vue` (card → editor; menu "⋯" ganhou "Ver página pública ↗" pra PUBLISHED, `window.open` em nova aba) e `pages/app/consultoria/clientes/index.vue` (só o nome era link; card inteiro agora navega, `<details>` do menu e link do site com `@click.stop`). Todo o resto verificado OK (tabelas de vagas dos 3 workspaces, candidatos empresa/hunter, candidaturas/salvas, diretórios públicos, perfil público). **Gap conhecido registrado, não corrigido**: nenhum desses cards/linhas `@click` é focável por teclado (padrão do app inteiro) — resolver de forma central na auditoria a11y pré-launch (ver PLANO F18).
- **F19 (2026-07-16), upload de capa/arquivos ANTES do primeiro save (pedido do Andres: "tengo que salvar rascunho para poder agregar imágenes... que ya desde el inicio se pueda")** — os endpoints de upload são `/portfolio/:id/cover|files` (precisam de id), e o editor bloqueava upload até um "Salvar rascunho" manual. **Fix em `PortfolioEditor.vue`**: `ensureDraft()` cria o rascunho sozinho no primeiro upload (com os valores atuais do form; título placeholder "Projeto sem título" refletido no campo se vazio — o backend exige `title` `IsNotEmpty`, e renomear depois regenera o slug com tombstone 301, ver `portfolio.service.ts::update`). **Ordem importa**: a URL só troca de `/novo` pra `/[id]` DEPOIS do upload terminar (`syncUrlToItem()` no fim do handler — o `router.replace` remonta a página e um replace precoce faria o arquivo recém-subido não aparecer); antes do replace, um `save(true)` flusha edições digitadas durante o upload (o autosave de 1.5s pode não ter disparado, e o remount as perderia). **Bug latente pré-existente descoberto e corrigido junto (revisando o backend)**: as respostas de `PATCH /portfolio/:id` e `POST /portfolio/:id/cover` carregam só a relation `tags` (`findOneOrFail`), **nunca `files`** — todo `item.value = updated` (autosave, publicar, despublicar, capa) apagava a galeria DA TELA até um reload (arquivos intactos no banco). Fix central: `mergeItem()` preserva `files` local quando a resposta não traz. Também adicionado feedback "Enviando..." no upload de capa (`uploadingCover` existia mas não era usado no template) e limpeza do `<input file>` de capa pós-upload (permite re-selecionar o mesmo arquivo). **F19b (mesmo dia)**: publicar agora redireciona pra lista (`navigateTo('/app/candidato/portfolio')` após o toast). **Validado E2E completo no Chrome (2026-07-16)** junto com F18 — upload de capa sem save → rascunho auto-criado + URL `/[id]` + capa no CDN; galeria; autosave pós-upload mantendo a galeria na tela (prova do fix do `mergeItem`); publicar → redirect pra lista; F18: clique no card → editor, menu "⋯" com stop, "Ver página pública ↗" interceptado chamando `window.open('/portfolio/<slug>','_blank','noopener')`. Projeto de teste excluído no final. Detalhe no PLANO (linhas F18/F19).
