# Plano de Desenvolvimento — vitrinepro-frontend-v2 + Gaps de Backend

> Mantido pelo agente `sprint-planner`. Specs: `../design-spec/`. Backend reutilizado: `../vitrinepro-bakend`.
> Coluna **Backend**: ✅ endpoint existe · 🟡 existe parcial/adaptar · ❌ não existe (ver §Backend)

---

## FASE 0 — Fundação (esta sessão / semana 1)
- [x] Scaffold Nuxt 4 + tokens + env copiadas do v1
- [x] UI kit base: Button, Input, Badge, Card, Modal, Drawer, Toaster
- [x] useApi (JWT, erros padronizados) + store auth + middleware
- [x] Layouts: marketing, auth, app (sidebar)
- [x] Páginas de referência: Home T01 (parcial), Login T12
- [x] `npm install` + `npm run dev` validados localmente
- [x] Completar UI kit: Select, Multi-select chips, Datepicker, campo moeda/telefone, Tabs, Stepper, Tabela, EmptyState, ConfirmDialog, Pagination, Avatar, KPI Card
- [x] Git init + primeiro commit (deploy preview Vercel: pendente — push para o remoto)

## FASE 1 — Público + Auth (aquisição/SEO) — Backend ✅ quase todo
| Tela | ID | Backend |
|---|---|---|
- [x] Cadastro 2 passos (persona) | T13 | ✅ `/auth/register` (isCompany ok; persona hunter ✅ B1 — persiste em `User.personas`)
- [x] Recuperar/redefinir senha | T14/T15 | ✅ B2 `POST /auth/forgot-password` + `POST /auth/reset-password/:token` (mock removido de `auth.forgotPassword`/`resetPassword`)
- [x] Home completa (prova social, depoimentos, carrossel) | T01 | 🟡 carrossel real `/vagas` + contadores reais `/stats/home` (B12 parcial); fees/placements/hunters ainda mock
- [x] /vagas + /vagas/[segmento] + filtros | T05 | ✅ `/vagas/radar` (q, segmento, cidade, tipo, modo, salário, ordem recent/relevance). Falta backend: order "maior salário/fee" e filtro fee (fee proxy=allowHunters no front) → B4
- [x] /vaga/[slug] + Modal Candidatura | T06 | ✅ apply (409/400/403), salvar, compartilhar, semelhantes, SEO JobPosting; backend: `findBySlugPublic` agora carrega `company`. Falta: valor do fee R$ + "Quero esta vaga" drawer T-H07 (B4); bloco fee mostra só "aceita hunters"
- [x] /precos | T04 | ✅ `/plans` (toggle mensal/anual, 4 cards, marketplace 25%, tabela comparativa, FAQ). CTA → /cadastro?plan=...&redirect=checkout (checkout = M2, pendente)
- [x] /para-empresas (calculadora fee), /para-candidatos | T02/T03 | — (marketing puro, calculadora client-side; sem backend)
- [x] Perfil público candidato | T09 | ✅ `/profile/:username` (+ portfolio/education/cv públicos; tabs Portfólio/Sobre/Formação; 404 oculto/empresa). Nota: banner "perfil oculto" p/ dono não dá (endpoint 404a oculto sem auth)
- [x] /hunters + /hunter/[username] | T07/T08 | ✅ B5 **validado E2E completo** `GET /hunters` (diretório, filtros specialty/city/verifiedOnly/order + `HunterMetrics`) + `GET /hunters/:username` (perfil). Selo "✓ Verificado" confirmado no diretório (com filtro ligado) e no perfil após aprovação real via fluxo B8
- [x] /empresa/[slug] | T10 | ✅ B6 **validado E2E completo** `GET /empresas/:slug` (conta isCompany, username como slug) + `vagasAbertas` (vagas PUBLISHED do dono). 404 e caminho feliz (`/empresa/andreshernandez9975`, conta real) confirmados no Chrome contra backend real
- [x] /processo/[token] | T11 | ✅ `/public/processo/:token` (layout limpo, etapa atual, score, notas, timeline; 410 front). Nota: snapshot não traz empresa nem expiresAt (footer sem "válido até"); backend responde 404 p/ expirado/revogado
- [x] /convite/[token] | T16 | ✅ B7 `GET /public/team-invite/:token` (sem auth) + `POST /team-invite/:token/accept` (com auth, e-mail deve bater); `POST /me/team/invite` agora gera `inviteToken` e envia e-mail real
- [x] 404/410 com tombstones | T18 | ✅ `/seo/*` (error.vue 404+410; tombstone em vaga/perfil → 410/301; /termos /privacidade /cookies /ajuda; banner de cookies). Textos legais = placeholder

## FASE 2 — Workspace Hunter (núcleo do produto)
- [x] Escolher perfil + onboarding hunter | T-C00/T-H01 | ✅ B1 (persona persistida em `User.personas`, ativável via `PATCH /profile/me/personas`); verificação de hunter ✅ B8 (ver T-H11)
- [x] Início "Minha mesa" | T-H02 | 🟡 slots reais (`/vagas/me/usage`); KPIs/mesa mock (B12)
- [x] Minhas Vagas + Editor + fluxo Publicar (slots, Modal Upgrade) | T-H03/T-H04 | ✅ `/vagas/me`, create/patch, `publish`, PLAN_LIMIT_REACHED→Modal Upgrade, autosave. ✅ B4: fee (%/R$), maxHunters, exclusivityDays no editor; publish bloqueia (400) se `allowHunters` sem fee
- [x] Pipeline kanban + drawer candidato + notas/score/histórico | T-H05 | ✅ kanban drag&drop (PATCH status), drawer (mover/rejeitar/nota etapa/histórico), estados DRAFT/CLOSED. Nota: listByVaga não retorna stageNotes/score → drawer escreve sem pré-carregar; origem sempre "Direta" (B3)
- [x] Modal Configurar Etapas | T-H06 | ✅ PATCH /me/pipeline-template (renomear, cor, reordenar, add/remover; rejected fixo)
- [x] Compartilhar processo + PDF | T-H10 | ✅ ShareProcessModal (gerar/copiar/revogar link público) + Baixar PDF, no drawer do candidato. Nota: sem endpoint de listagem → lista só os links da sessão; URL montada com frontendUrl (backend retorna domínio prod); QR pendente (sem lib)
- [x] Marketplace de vagas + termos | T-H07 | ✅ B4: `pages/app/hunter/marketplace.vue` (novo, nav "Marketplace") — reusa `GET /vagas/radar?allowHunters=true` (filtros segmento/fee mínimo/ordenar por maior fee); `VagaCard` mostra pill de fee. Termos aceitos no drawer "Quero esta vaga" de `/vaga/[slug]` (fee/exclusividade/máx. hunters + checkbox obrigatório) → `POST /vagas/:id/hunter-interest {termsAccepted:true}`
- [x] **Meus Candidatos + Submeter candidato (3 passos)** | T-H08 | 🟡 **backend + front v1 feitos**. Front: `/app/hunter/candidatos` (lista+busca+badge LGPD+drawer detalhe), `AddCandidateModal` (cadastrar fantasma), `SubmitCandidateModal` (stepper 3 passos, trata 400/403/409). Backend: módulo `hunter-candidates`, `POST /vagas/:id/submissions` (limite 5/hunter + trava configurável por vaga — B4), migração `1748600000000`. **Faltam (v2):** aba "Buscar na VitrinePro" (dep. B13), upload de CV, tags/filtros, e-mail real (B14); seletor de vaga usa só as vagas próprias publicadas
- [ ] Placements & Ganhos | T-H09 | 🟡 backend ✅ B9 (entidade+P1-P4+crons); falta front (modal marcar contratado, drawer confirmar/contestar, timeline, dashboard "Ganhos"/"Placements em andamento" hoje hardcoded 0 em `/app/hunter`)
- [x] Perfil de hunter (edição) | T-H11 | ✅ B5+B8 **validado E2E completo** — `pages/app/hunter/perfil.vue`: self-edit (headline/bio/cidade/anos/especialidades via `PATCH /profile`) + painel de verificação (upload PDF + LinkedIn + solicitar → fila admin → aprovar/recusar com motivo). Gates de "verificado" em marketplace/candidatos/vaga-slug/submissão confirmados desbloqueando após aprovação real

## FASE 3 — Workspaces Candidato + Empresa
- [ ] Candidato: Início, Radar (desktop+swipe), Candidaturas, Salvas, Perfil, Portfólio, CVs, Formação | T-C01..09 | ✅ tudo existe (saved-vagas, saved-filters, cv, education, portfolio)
- [ ] Empresa: onboarding, vagas, pipeline com mascaramento, hunters (aceitar/avaliar), faturas, página | T-E01..09 | 🟡 vagas/pipeline ✅; mascaramento ✅ B4 (backend + UI no drawer do candidato — só falta o workspace Empresa em si, Fase 3 não construída, pra configurar a etapa de liberação pela UI); avaliações ❌ B10; faturas ❌ B11
- [ ] Consentimento LGPD do candidato (notificação + modal) | T-C09/N | ❌ B3

## FASE 4 — Consultoria + Placement + Monetização real
- [ ] Consultoria: criação, início KPIs, vagas time, pipeline geral, clientes, membros, faturamento, config | T-T01..08 | ✅ teams/companies/seats; KPIs ❌ B12
- [ ] Fluxo Placement completo (P1–P4) | `design-spec/06_ADMIN_E_FLUXOS_TRANSVERSAIS.md §P` | 🟡 backend ✅ B9 (validado E2E); falta front
- [ ] Assinatura + Checkout (Pix/cartão/boleto) + Modal Upgrade global | M1–M3 | 🟡 checkout/confirm MOCK → gateway ❌ B11
- [ ] Indicações (cupons) | M4 | ✅ coupons
- [ ] Conta: dados, segurança, notificações, privacidade | 06 §C | 🟡 sessões/export ❌ B13

## FASE 5 — Admin + polish
- [ ] Admin: visão geral, verificações, disputas, placements, cupons, usuários | A1–A6 | 🟡 cupons ✅; **verificações (A2) ✅ B8** `pages/app/admin/verificacoes.vue` (fila `PENDING`, aprovar/recusar com motivo obrigatório) + `middleware/admin.ts` (role-gating, novo) — validado E2E completo (aprovar E recusar testados com conta real promovida a `role=ADMIN` via SQL direto, não existe rota de auto-promoção); resto ❌ B9/B12
- [ ] Sino de notificações + busca cmd+K | 06 §N | ❌ B13 (notificações in-app)
- [ ] E2E Playwright dos fluxos de dinheiro; Lighthouse ≥90 nas públicas

---

# §CABOS SOLTOS — Auditoria completa 2026-07-05 (frontend + backend)

> Varredura sistemática pedida pelo Andres após descobrir que o logout nunca foi ligado à UI.
> Método: grep de TODO/mock/stub nos dois repos, cruzamento de todos os links internos (`to=`/`navigateTo`/`href`) contra as páginas existentes, leitura dos controllers vs design-spec.
> Códigos: **F#** = frontend v2 · **B#** continua a tabela §BACKEND abaixo (B17–B21 novos).

## Críticos (quebram fluxo de usuário real hoje)

| # | Gap | Descrição | Fase sugerida |
|---|---|---|---|
| [x] ~~**F1**~~ ✅ | **Menu do avatar + Sair** | **FEITO** (2026-07-05). `layouts/app.vue`: dropdown com click-outside (`onClickOutside`), "Minha conta" (linka para `/app/hunter/perfil` se persona HUNTER, senão `/app`) e "Sair" ligado a `auth.logout()`. Spec 00 §3.9 implementada. | agora |
| [x] ~~**F2**~~ ✅ | **Redirect pós-login para rotas que não existem** | **FEITO** (2026-07-05). Criadas `pages/app/empresa/index.vue` e `pages/app/candidato/index.vue` (placeholders "workspace em construção" com CTA para página pública/vagas — correção real ainda é FASE 3). `pages/app/index.vue` agora roteia por `personas` (HUNTER/CANDIDATO) com fallback seguro em vez de assumir hunter sempre. | agora |
| [x] ~~**F9**~~ ✅ | **CTA de /precos leva a 404** | **FEITO** (2026-07-05). `onCta` em `precos.vue` não manda mais para `/app/conta/assinatura/checkout` (inexistente) — mostra toast "checkout chegando em breve" e leva para `/app` (logado) ou `/cadastro?redirect=/precos` (deslogado). Checkout real = M2, ainda pendente. Open-redirect de `?redirect=` corrigido junto (ver nota abaixo). | agora |

## Frontend — telas/ligações faltantes

| # | Gap | Descrição | Fase |
|---|---|---|---|
| [x] ~~**F3**~~ ✅ | /app/escolher-perfil (T-C00) | **FEITO** (2026-07-06). `pages/app/escolher-perfil.vue` (novo): cards HUNTER/CANDIDATO com badge "já ativo" para personas existentes, `auth.activatePersona()` idempotente + `vp_last_workspace` + navega pro workspace. Conta `isCompany` é redirecionada pra `/app/empresa` (sem escolha, persona fixa). `app/index.vue` agora manda pra cá (em vez de default silencioso hunter) quando `personas` vazio/null. | 3 |
| [ ] **F4** | WorkspaceSwitcher | Spec 00 §3.9 (dropdown Candidato/Hunter/Consultorias/Empresa/Admin + persistir contexto no backend antes de navegar). Nada construído; backend `set-active-context` já existe (`profile/dto/set-active-context.dto.ts`). | 3 |
| [x] ~~**F5**~~ ✅ | Sino de notificações morto | **FEITO** (2026-07-06, parte do gap — sino). `layouts/app.vue`: botão Bell agora abre popover (click-outside, mesmo padrão do F1) com `UiEmptyState` "Nada por aqui ainda". **Ainda falta**: busca global cmd+K (mesma linha do spec, requer B13/índice de busca — não fazia parte do escopo do sino, fica pra FASE 5). | 3–5 |
| [x] ~~**F6**~~ ✅ | Footer com links 404 | **FEITO** (2026-07-05). `pages/sobre.vue` (institucional) e `pages/contato.vue` (formulário mailto: + canais de vendas/suporte/ajuda) criadas, layout default aplicado automaticamente. Não há endpoint de contato no backend — formulário é client-side (`mailto:`); se precisar de um form real, abrir gap novo. | 1 |
| [x] ~~**F7**~~ ✅ | Detalhe de portfólio inexistente | **FEITO** (2026-07-06). `pages/portfolio/[slug].vue` (novo): capa, título/subtítulo, meta (status/cliente/ano/duração/role), tags, link externo, galeria de arquivos (imagens/PDF), rodapé com autor + link de volta ao perfil. Consome `GET /portfolio/:slug` (já existia). **Bug de segurança encontrado e corrigido de passagem**: esse endpoint é público (sem auth obrigatória) e devolvia a relation `user` crua com `password` — pior que o B19 por não exigir login nenhum. `PortfolioService.findBySlug` agora remove `password`/`oauthId`/`avatarKey`/`bannerKey`/tokens antes de devolver. | 3 |
| [x] ~~**F8**~~ ✅ | `/vagas?fee=true` ignorado | **Já estava feito** (checado em 2026-07-05, nota do audit estava desatualizada). `VagasListing.vue:24` já lê `route.query.fee === 'true'` e filtra por `allowHunters` — CTA da home funciona ponta a ponta. | 1 |
| [ ] **F10** | Verificação de e-mail (T17) | Backend **pronto** (**B17** feito em 2026-07-06): `POST /auth/verify-email/:token` + `POST /auth/resend-verification`. Falta só a página `/verificar-email/[token]` (chama o endpoint, mostra sucesso/erro, CTA reenviar) — item de front autocontido, sem risco pro resto do app. | 2 |
| [ ] **F11** | ShareProcessModal incompleto | ~~Sem QR (sem lib) e a lista de links só vive na sessão~~ **Listagem resolvida via B18** (2026-07-06): `ShareProcessModal.vue` agora carrega do backend ao abrir. Falta só o QR code (sem lib ainda). | 2 |
| [x] ~~**F12**~~ ✅ | Textos legais placeholder | **FEITO** (2026-07-06). `termos.vue` (12 seções: aceitação, conta, personas, uso aceitável, vagas/candidaturas, marketplace/fee/comissão 25%, pagamentos, propriedade intelectual, responsabilidade, rescisão, alterações, foro), `privacidade.vue` (11 seções LGPD: controlador, dados coletados, finalidade, compartilhamento incl. mascaramento B4, retenção, direitos do titular, segurança, cookies, menores, alterações, contato/DPO), `cookies.vue` (4 seções). Conteúdo redigido com base no funcionamento real da plataforma (fee/comissão, personas, mascaramento, planos). `LegalDoc.vue`: disclaimer trocado de "substitua antes de publicar" para "recomendamos revisão por advogado antes do lançamento". **Não é substituto de revisão jurídica real** — sinalizado no próprio documento e aqui. | pré-launch |

**Mocks visíveis já rastreados (não são gaps novos, só referência):** contadores fees/placements/hunters da home + KPIs "Minha mesa" do hunter → B12; carrossel da home cai em `vagasMock` quando o backend está offline/vazio (comportamento intencional de fallback, `index.vue:19`).

---

# §BACKEND — O que falta no NestJS (reutilizando `vitrinepro-bakend`)

**Mantém-se intacto (já serve o v2):** auth/OAuth, profile, portfolio, education, cv, tags, vagas + publish-ledger, pipeline-templates, vaga-applications (notas/score/histórico), process-share/PDF, teams/seats, companies, saved-vagas/filters, search, coupons, seo/tombstones, storage.

| # | Gap | Descrição | Fase |
|---|---|---|---|
| [x] ~~**B1**~~ ✅ | **Persona/role de produto** | **FEITO e validado E2E** (2026-07-06). Enum `UserPersona` (`CANDIDATO`/`HUNTER`/`EMPRESA`) + coluna `User.personas` (`simple-array`, migração `1749100000000-b1-user-personas` com backfill `EMPRESA`/`CANDIDATO` para contas existentes — **nota:** dev roda com `synchronize:true`, então a coluna já existia via sync antes da migração rodar; migração ainda precisa ser aplicada em prod/CI). `POST /auth/register` aceita `persona?: 'CANDIDATO'\|'HUNTER'` (empresa sempre `[EMPRESA]`; hunter acumula `[CANDIDATO,HUNTER]`); `login`/`register` retornam `personas` no `user`. Novo `PATCH /profile/me/personas` (`ActivatePersonaDto`, `JwtAuthGuard`) ativa CANDIDATO/HUNTER de forma idempotente; bloqueia EMPRESA (403) e bloqueia contas `isCompany` de ativarem outras personas (403). Front: `auth.ts` (`User.personas`, `register(persona)`, novo `activatePersona()`), `cadastro.vue` envia a persona escolhida no registro por e-mail, `auth/callback.vue` chama `activatePersona` após login OAuth (a persona só pode ser persistida depois que a conta OAuth já existe). Validado no Chrome com a conta real `testeia@getnada.com`: ativação HUNTER, acumulação CANDIDATO+HUNTER, idempotência, e os dois caminhos 403 (hunter tentando EMPRESA; conta empresa tentando HUNTER). **Dívida encontrada (pré-existente, não é regressão do B1):** `GET /profile/me`, `PATCH /profile` e `PATCH /profile/me/personas` retornam a entidade `User` crua, incluindo o hash de `password` — nenhum desses endpoints estava removendo campos sensíveis antes do B1; considerar como item de limpeza (B16 ou gap novo). | 1 |
| [x] ~~**B2**~~ ✅ | **Reset de senha** | **FEITO e validado E2E** (2026-07-05). `POST /auth/forgot-password` (resposta sempre genérica; contas OAuth sem senha não geram token) + `POST /auth/reset-password/:token` (token 1h, uso único, hex 24 bytes; 404 se não existe, 410 se expirado). Front `T14/T15` ligado (mock removido). E-mail real via B14 (`MailService.sendPasswordReset`). Andres recebeu o e-mail, redefiniu a senha da conta `testeia@getnada.com` de verdade e confirmou — fluxo completo ponta a ponta OK. | 1 |
| [x] ~~**B3**~~ ✅ | **Submissão de candidatos por hunter** | **FEITO (backend)** — módulo `hunter-candidates`: `hunter_candidates` (talent pool/CRM, candidato fantasma), consentimento LGPD por token (e-mail=**stub**, real depende de B14), `POST /vagas/:id/submissions` (limite 5/hunter + trava 90d — RN-NOVA-01/02), `source`/`submittedByHunterId`/`hunterCandidateId` na application. Migração `1748600000000`. Falta: e-mail real (B14) + upload de CV do candidato + front T-H08. | 2 |
| [x] ~~**B4**~~ ✅ | **Marketplace/fee na vaga** | **FEITO e validado E2E** (2026-07-06). Migração `1749200000000-b4-marketplace-fee`: `vagas.feePercent`/`feeAmount` (nullable, ao menos um exigido em `POST /vagas/:id/publish` quando `allowHunters=true` — 400 caso contrário), `vagas.maxHunters` (default 5) e `vagas.exclusivityDays` (default 90, agora usado por `HunterCandidatesService` na trava RN-NOVA-02 em vez da constante fixa), `hunter_interests.termsAcceptedAt`, `users.hunterContactRevealStageOrder` (default 2 = 3ª coluna). `POST /vagas/:id/hunter-interest` exige `{termsAccepted:true}` (`ExpressInterestDto`, 400 caso contrário); `PATCH /vagas/:id/hunter-interests/:hunterId` bloqueia aceitar além de `maxHunters` hunters simultâneos ACEITOS (409, idempotente — reaceitar o mesmo não conta 2x). **RN-NOVA-03 (mascaramento)**: `VagaApplicationsService.listByVaga` mascara `snapshotEmail`/`snapshotPhone` (+ `contactMasked:true`) de candidaturas `source=HUNTER` cujo `pipelineStage` (resolvido via `order` na `pipeline_template` do dono) seja menor que `User.hunterContactRevealStageOrder`; configurável via `PATCH /profile`. `GET /vagas/radar` ganhou `allowHunters`/`feeMin`/`order=fee` (marketplace de hunters). Front: `VagaEditor.vue` (fee %/R$, maxHunters, exclusividade, valida antes de publicar), `pages/app/hunter/marketplace.vue` (novo, T-H07), `VagaCard.vue` (pill de fee), `pages/vaga/[slug].vue` (drawer "Quero esta vaga" com termos reais + `POST hunter-interest`), `CandidateDrawer.vue` (badge "Indicado por hunter" + contato 🔒 quando `contactMasked`). Testado no Chrome contra o backend real: publish bloqueado sem fee (400) → liberado após definir fee → bloqueado de novo por `PLAN_LIMIT_REACHED` (comportamento correto, conta free já tinha usado o slot); `hunter-interest` sem termos (400), com termos (201, `termsAcceptedAt` setado), duplicado (409), aceite (200), cap de `maxHunters` confirmado via DTO (`@Min(1)` bloqueia 0, então o "reject→cap" foi validado indiretamente); mascaramento confirmado na fronteira exata (etapas 0/1 mascaradas, etapa 2 "Abordados" revela, exatamente como a 3ª coluna do design-spec); radar com `allowHunters=true&order=fee` retornou só vagas certas, ordenadas por fee. **Dívida**: cap de `maxHunters` com uma 2ª conta hunter real tentando aceitar além do limite não foi exercitado (só há 2 contas de teste); UI de configurar `hunterContactRevealStageOrder` ainda não existe (sem workspace/Conta Empresa) — hoje só ajustável via `PATCH /profile` direto. | 2 |
| [x] ~~**B5**~~ ✅ | **Perfil público de hunter** | **FEITO e validado E2E completo** (2026-07-06). Novo módulo `hunters`: `User.hunterSpecialties`/`hunterYearsExperience` (editáveis via `PATCH /profile`). `GET /hunters` (diretório T07: filtros `specialty`/`city`/`verifiedOnly` default true/`order`, paginado) e `GET /hunters/:username` (perfil T08) — ambos só `isVisible && !isCompany && personas LIKE '%HUNTER%'`. `HunterMetrics` (indicações/taxa de aproveitamento/tempo até abordagem/avaliação média) computadas de `VagaApplication` — proxies até B9/B10 existirem. Front: `pages/hunters.vue` + `pages/hunter/[username].vue` + `types/hunter.ts`. Selo "✓ Verificado" (via B8) confirmado no diretório e perfil com dados reais. | 2 |
| [x] ~~**B6**~~ ✅ | **Página pública de empresa** | **FEITO e validado E2E** (2026-07-06). `GET /empresas/:slug` em `ProfileService.getPublicCompany` (novo `CompanyPublicController`, path plano fora do prefixo `/profile`) — usa `username` como slug, exige `isCompany=true` (inverso de `getPublicProfile`), 404 para oculta/candidato/inexistente. `VagasService.findPublicByOwner` traz as vagas `PUBLISHED` do dono (projeção enxuta). Front `pages/empresa/[slug].vue` feito. Testado com conta real (`andresempresa@getnada.com`, username `andreshernandez9975`) — nome, indústria e estado vazio de vagas corretos. **Falta incremental**: testar `vagasAbertas` com dados reais — não há workspace Empresa (Fase 3) construído ainda para publicar uma vaga por essa conta. | 1 |
| [x] ~~**B7**~~ ✅ | **Convite por token** | **FEITO.** `TeamMember.inviteToken` (hex 24 bytes, `select:false`) gerado em `POST /me/team/invite`; e-mail real via B14 (`MailService.sendTeamInvite`) com link `/convite/[token]`. Rotas: `GET /public/team-invite/:token` (sem auth) + `POST /team-invite/:token/accept` (com auth; exige e-mail igual ao convite; token one-time). Fluxo antigo por `memberId` continua para quem já está logado. Front `pages/convite/[token].vue` feito. **Falta**: validar criação+aceite ponta a ponta com conta plano TEAM/ENTERPRISE (não existe em `qa-test-accounts.json`). | 1 |
| [x] ~~**B8**~~ ✅ | **Verificação de hunter** | **FEITO e validado E2E completo** (2026-07-06). Migração `1749300000000-b5-b8-hunter-profile-verification`: `User.verificationStatus` (`NONE\|PENDING\|APPROVED\|REJECTED`), `verificationDocs` (jsonb), `verificationLinkedinUrl`, `verificationRequestedAt`/`verificationDecidedAt`/`verificationRejectionReason`. `POST /profile/me/verification/documents` (upload PDF) → `POST /profile/me/verification/submit` (`PENDING`) → admin `GET /admin/hunters/verifications` + `approve`/`reject` (`RejectVerificationDto{reason}`, e-mail real nos dois casos). **Gate do marketplace**: `hunter-interests.express()` e `hunter-candidates.submitToVaga()` bloqueiam (`ForbiddenException{code:'HUNTER_NOT_VERIFIED'}`) hunters não-`APPROVED`. Front: `pages/app/hunter/perfil.vue` (T-H11, self-edit + upload + submit), `pages/app/admin/verificacoes.vue` (A2, fila+aprovar+recusar) + `middleware/admin.ts` (novo, role-gating), gates visuais em `marketplace.vue`/`candidatos.vue`/`vaga/[slug].vue`/`SubmitCandidateModal.vue`. Ciclo completo testado no Chrome com conta real promovida a admin via SQL (`role='ADMIN'`, sem rota de auto-promoção): submeter → recusar (motivo) → reenviar → aprovar → selo aparece, gates desbloqueiam. **3 bugs encontrados e corrigidos durante a validação** (2 backend: boolean de query string, `select` faltando `personas`; 1 frontend: refs de formulário não reativos ao `auth.user` assíncrono — ver CLAUDE.md de cada repo). | 2 |
| [x] ~~**B9**~~ ✅ | **Placements** | **FEITO e validado E2E completo** (2026-07-06). Spec confirmada em `design-spec/06_ADMIN_E_FLUXOS_TRANSVERSAIS.md §P`. Novo módulo `placements`: entidade `Placement` (migração `1749600000000-b9-placements`) 1:1 com `vaga_applications` — salário contratado, regime (CLT/PJ), data início; fee/garantia só existem quando a candidatura veio de hunter (`source=HUNTER`+`submittedByHunterId`). Fee = `vaga.feePercent`% do salário (fallback `feeAmount` flat) — **split do fee 75% hunter / 25% plataforma** (confirmado com número real: fee R$3.500 → hunter R$2.625 / plataforma R$875, batendo exato com o exemplo do design-spec; o "75/25" é a partilha do fee entre hunter/plataforma, não do salário). Garantia fixa de **90 dias** a partir da confirmação. Fluxo: **P1** `POST /applications/:id/placement` (dono da vaga/delegado de time B15/admin; exige `termsAccepted:true` quando há hunter) → contratação direta (sem hunter) nasce já `CONFIRMED` sem fee/garantia (só registro de auditoria p/ B12); com hunter nasce `HIRED` e dispara e-mail `sendPlacementHired` → **P2** `POST /placements/:id/confirm` (só o hunter da indicação) ou `POST /placements/:id/contest` (abre `DISPUTED`, e-mail à empresa) → **P3** `GET /placements/:id/timeline` (steps Contratado/Confirmado/Em garantia/Fee liberado, acesso hunter/dono-delegado/admin, 403 pra terceiros) → **P4** `POST /placements/:id/departure` (só dentro da garantia, vira `GUARANTEE_BROKEN`, notifica hunter) → reposição = novo `POST /applications/:id/placement` com `replacesPlacementId` apontando pro placement quebrado (linka os dois, original vira `REPLACED`) — **decisão de escopo confirmada com Andres**: só reposição gratuita, sem fluxo de estorno proporcional. Admin: `POST /placements/:id/resolve-dispute` (`CONFIRM`/`CANCEL`). Dois crons diários (`@Cron`, mesmo padrão do `seo.service.ts`) automatizam **auto-confirm 7d** (`HIRED`→`CONFIRMED` se o hunter não respondeu) e **liberação de fee** (`CONFIRMED`→`FEE_RELEASED` quando a garantia expira sem quebra); endpoint `POST /placements/:id/qa-force-sweep` (dev-only + ADMIN) força essas duas transições sob demanda pra validar a mecânica sem esperar os dias reais (mesmo espírito do "token em dev" do B3/B7/B17). E-mails novos em `mail.templates.ts`/`mail.service.ts` para os 5 eventos do fluxo. **2 bugs reais encontrados e corrigidos durante a validação** (fora do código do B9 em si, mas bloqueavam tudo): (1) `app.module.ts` importava `pipeline-templates/pipeline-template.module` (singular) quando o arquivo real é `pipeline-templates.module.ts` (plural) — import quebrado pré-existente, corrigido; (2) **entidades do TypeORM são registradas explicitamente em `database.config.ts`** (array `entities`), não só via `TypeOrmModule.forFeature` no módulo — `Placement` faltava nesse array, causando 500 genérico em qualquer query (tabela nunca sincronizada). Testado ponta a ponta contra o backend real: P1 sem termos (400), com termos (201, fee/split batendo com a spec), duplicado (409); P2 confirm (201, `guaranteeEndsAt`=+90d exato) e contest (201, `DISPUTED`); P3 timeline com steps corretos e 403 pra ator não-relacionado (testado com conta `b15recruiter2`, fora da vaga); P4 departure (201, `GUARANTEE_BROKEN`) + reposição linkada (original→`REPLACED`, nova→`CONFIRMED` sem fee por ser contratação direta); admin resolve-dispute `CONFIRM` (201); `qa-force-sweep` pra auto-confirm (`autoConfirmed:true`) e fee-release (`FEE_RELEASED`), 403 pra não-admin. Candidatos/contas de teste descartáveis (Bruno/Carla/Diego) ficaram no banco de dev, mesma situação já registrada em B17/B18 (sem endpoint de exclusão de usuário). **Falta (fora de escopo desta rodada)**: front (modal "Marcar contratado", drawer de confirmação/contestação do hunter, timeline visual, "Minha mesa"/dashboard usando dados reais em vez do placeholder `R$0`/`0` já visível em `/app/hunter`) — ver F10/F11/F4 e B12. | 4 |
| [ ] **B10** | Avaliações de hunter | `reviews` (1–5 + comentário, 1 por placement, imutável — RN-NOVA-07), agregadas no perfil B5. | 3 |
| [ ] **B11** | **Pagamentos reais** | Trocar mock `POST /subscriptions/:id/confirm` por gateway com webhook (Asaas ou Pagar.me — ambos têm split nativo p/ fee do hunter). Faturas de fee, Pix/cartão/boleto, inadimplência bloqueia publish. | 4 |
| [ ] **B12** | Agregações/KPIs | Endpoints de dashboard (hunter: ganhos/mês; consultoria: pipeline overview, atividade; admin: GMV/MRR; home: contadores públicos). 🟡 **home feito:** `GET /stats/home` (openVagas, professionals, companies) em `src/stats/`. Falta: dashboards hunter/consultoria/admin. | 2–5 |
| [ ] **B13** | Notificações | Tabela `notifications` + endpoints (sino), preferências por evento; sessões ativas + export LGPD. | 3–5 |
| [x] ~~**B14**~~ ✅ | **E-mail transacional** | **FEITO e validado E2E.** Módulo `mail` (Resend via `fetch`, sem dep.; modo STUB sem key). Consentimento de B3 envia de verdade → página pública `/consentimento/[token]`. **Domínio `send.v8pro.com.br` verificado no Resend** e `MAIL_FROM=no-reply@send.v8pro.com.br` — e-mail entregue e renderizado OK numa caixa real (getnada) em 2026-07-05. **Falta (incremental):** ligar B2 (reset) e B7 (convite) ao MailService; templates de submissão/etapa/placement/fatura. | 1 |
| [x] ~~B15~~ ✅ | Delegação de time em candidaturas | **FEITO e validado E2E** (2026-07-06). Novo `TeamContextHelper.getTeamForUser(userId)` (resolve o time de um usuário, seja como OWNER ou membro ACTIVE) + `TeamContextHelper.canManageAsTeamLead(vagaCreatorId, actorId)` (o time do criador da vaga existe? o ator é o OWNER desse time, ou um MANAGER ACTIVE nele?) — ambos novos, centralizados no helper de time (mesmo padrão de `getTeamContext`/`isOwnerOrManager` já usados por Vagas/Teams/Plans). `VagaApplicationsService` ganhou `assertCanManageVaga()` (privado) substituindo os 5 checks repetidos de `createdById === actorId` em `listByVaga`, `updateStatus`, `updateGeneral`, `updateStageNotes` e `getHistory` — agora cada um também aceita OWNER/MANAGER do time do dono da vaga, além do criador original e do ADMIN (RECRUITER do mesmo time que não seja o criador continua **sem** acesso — delegação é só pra liderança do time, conforme descrito no gap). `VagaApplicationsModule` importa `TeamsModule` pra injetar o helper. **Não incluído nesta rodada** (fora do escopo literal do gap): `process-share` (share-links/PDF) ainda só checa `createdById`/admin — se quiser delegação de time aí também, é um gap novo separado. Testado contra o backend real com o time "Time de B15" (`qa-test-accounts.json`): `b15recruiter` cria+publica vaga → candidato se aplica → `b15owner` (200), `b15manager` (200) e o próprio `b15recruiter`/criador (200) conseguem `listByVaga`; `testeia@getnada.com` (fora do time) → 403 (sem regressão). MANAGER moveu a etapa (`PATCH .../status` → 200), OWNER salvou uma nota de etapa (`PATCH .../stage-notes/:key` → 200), MANAGER leu o histórico (`GET .../history` → 200, 1 entrada). **Caso negativo confirmado**: criei um segundo RECRUITER no mesmo time (não é o criador, não é OWNER/MANAGER) — `listByVaga` → 403, como esperado (a liderança do time tem acesso, um colega RECRUITER comum não). Vaga de teste descartável deletada ao final. | 2 |
| [x] ~~B16~~ ✅ | Limpeza (parcial) | **FEITO** (2026-07-06) para os dois itens confirmados: (1) campos de "Service offering" do portfólio (`isService`/`serviceType`/`actionButton`/`servicePrice`/`publicationDurationDays`) removidos de `portfolio.entity.ts`, `create-portfolio.dto.ts` (e `UpdatePortfolioDto`, que herda), `portfolio.service.ts` (create/update/list) e também de `search.service.ts`/`search-query.dto.ts` (filtro `isService` no /search — não fazia parte do escopo original do B16, mas usava os mesmos campos, então entrou no cleanup) — migração `1749500000000-b16-cleanup-service-fields` (drop columns). Confirmado zero uso no frontend antes de remover (grep) e o frontend nem tem UI de criar/editar portfólio ainda (só a página pública de detalhe), então risco zero. Validado com o backend real: `GET /portfolio`, `GET /search`, `POST/PATCH/DELETE /portfolio/:id` — todos OK sem os campos. (2) `PlanLimitGuard` confirmado 100% morto (não usado via `@UseGuards` em lugar nenhum — `vagas.module.ts` nem importa mais `PlansModule`, o enforcement de limite migrou pro `VagasService.publish()`/ledger) — removido de `plans.module.ts` (providers/exports). **Pendência**: o arquivo `src/plans/plan-limit.guard.ts` em si não pôde ser deletado (ambiente do Claude não tem permissão de exclusão de arquivo nesta sessão — `rm` retornou "Operation not permitted" mesmo pra um arquivo de teste) — o arquivo fica órfão (sem nenhum import), inofensivo, mas o Andres precisa apagá-lo manualmente. "Enum deprecated" e "padronizar migrações (sem synchronize)" da descrição original **não foram feitos** — não achei um enum deprecated óbvio no código, e "padronizar migrações" é uma tarefa maior (rodar todas as migrações pendentes em vez de depender de `synchronize:true`) que fica pra uma rodada dedicada. | 0 |
| [x] ~~**B17**~~ ✅ | **Verificação de e-mail** (novo, auditoria 2026-07-05) | **FEITO e validado E2E** (2026-07-06). Migração `1749400000000-b17-email-verification`: `User.emailVerified` (default `false`) + `emailVerificationToken`/`emailVerificationExpiresAt` (`select:false`, hex 24 bytes, expira 24h; em dev a coluna já existe via `synchronize:true` — mesma nota do B1, migração ainda precisa rodar em prod/CI). `register()` gera o token e dispara `MailService.sendEmailVerification` (não bloqueante — falha de envio não impede o cadastro); contas OAuth (Google/LinkedIn) nascem com `emailVerified:true` (provedor já validou). Novo `POST /auth/verify-email/:token` (público, 404 se inválido, 410 se expirado, uso único) e `POST /auth/resend-verification` (autenticado, idempotente — resposta genérica se já verificado, reinvalida o token anterior). **Sem gate bloqueante** em nenhum fluxo existente por decisão de escopo (evita criar dependência nova pro front nesta rodada) — fica só informativo por enquanto; gate futuro (ex. exigir verificado pra publicar vaga) é decisão de produto em aberto. Em dev (`NODE_ENV!=='production'`) o token volta no próprio response (`devEmailVerificationToken`) — mesmo padrão do `consentToken` do B3 — pra dar pra validar sem caixa de e-mail real. Testado via fetch direto contra o backend real: registro → token devolvido → `emailVerified:false` antes → verify 200 → `emailVerified:true` depois → reuso do mesmo token 404 → token inválido 404 → fluxo de reenvio (token antigo invalidado, novo token verifica, reenviar já-verificado retorna mensagem genérica sem token novo) → `resend-verification` sem auth = 401. Duas contas de teste descartáveis ficaram no banco de dev (sem endpoint de exclusão de usuário no backend). Par do **F10** (página `/verificar-email/[token]`, ainda não construída). | 2 |
| [x] ~~B18~~ ✅ | Listagem de share-links (novo) | **FEITO e validado E2E** (2026-07-06). Novo `GET /applications/:id/share` (`ProcessShareService.listLinks`, mesma autorização owner/admin do create/revoke) retorna todos os links (ativos/expirados/revogados) com `expiresAt`/`revokedAt`/`isActive`. `create()`/`listLinks()` agora montam a URL com `FRONTEND_URL` (mesma env do MailService) em vez do antigo `PUBLIC_BASE_URL` fixo (v8pro.com.br) — endpoint público é servido pelo front, não fazia sentido apontar pro domínio errado. `getPublicProcess` ganhou `vaga.companyName` (client company vinculada ou `companyName` da conta dona da vaga, quando ela mesma é empresa) — exibido em `/processo/[token].vue`. `ShareProcessModal.vue` reescrito: carrega a lista do backend ao abrir (`watch(open)`) em vez de manter só em memória de sessão, mostra badge Ativo/Expirado/Revogado + data de expiração real, esconde ações de copiar/revogar em links inativos. Testado contra o backend real: criei vaga+candidatura descartáveis com a conta empresa de teste (`ANDRES H TESTE`), gerei link → `companyName` correto no snapshot público (confirmado visualmente na página `/processo/[token]`) → listagem antes/depois de criar → 403 pra quem não é dono da vaga → revogar → link revogado retorna 404 no público. Testado também na UI real (conta hunter, candidata Ana Souza): abrir modal → gerar link → fechar e reabrir → **link persiste** (prova do fix) → revogar → badge muda pra "Revogado" e ações somem. Dados de teste descartáveis limpos (vaga deletada); a candidata de teste avulsa (sem endpoint de exclusão de usuário) fica no banco, mesma situação já registrada no B17. | 2 |
| [x] ~~**B19**~~ ✅ | **Sanitização de respostas — hash de senha vazando** (promovido de nota) | **FEITO** (2026-07-05). `ProfileService.sanitize()` (novo, privado) remove `password`/`passwordResetToken`/`passwordResetExpiresAt` antes de devolver a entidade — aplicado em `getMyProfile`, `updateProfile`, `activatePersona` (incl. branch idempotente), `uploadAvatar`, `uploadBanner`, `setActiveContext`. `GET /auth/profile` já era seguro (retornava só campos escolhidos manualmente, não a entidade crua). Verificado por leitura direta do arquivo final (`sanitize` chamado nos 6 métodos, sem `return user`/`return this.usersRepository.save(user)` cru restante). | agora |
| [x] ~~B20~~ ✅ | Hardening de auth (novo) | **FEITO e validado E2E** (2026-07-06). `@nestjs/throttler` e `helmet` adicionados ao `package.json` (Andres rodou `npm install` de verdade na máquina dele — o sandbox do Claude não consegue instalar pacotes reais, só edita código-fonte). `ThrottlerModule.forRoot([{name:'default', ttl:60_000, limit:100}])` global (`APP_GUARD`) em `app.module.ts` — 100 req/min por IP como teto geral. `AuthController` ganhou `@Throttle({default:{limit:5, ttl:60_000}})` em `register`/`login`/`forgot-password` (5/min, bem mais estrito, mira especificamente brute-force/enumeração). `main.ts`: `app.use(helmet({crossOriginResourcePolicy:{policy:'cross-origin'}}))` — a exceção do CORP é necessária porque uploads/avatares servidos pelo backend são consumidos pelo front (outra origem). Testado com o servidor real do Andres rodando: headers `X-Content-Type-Options`/`X-Frame-Options`/`Strict-Transport-Security`/`Cross-Origin-Opener-Policy`/`Referrer-Policy` presentes em qualquer resposta; 8 tentativas seguidas de `POST /auth/login` com credencial errada → 5×`401` (processadas normalmente) depois 3×`429` (bloqueado) — rate-limit confirmado funcionando. **Não incluído nesta rodada**: throttle estrito em `/auth/verify-email`/`/auth/reset-password` (tokens são de uso único e expiram, risco menor, mas poderia entrar num gap de hardening futuro se quiser mais fechado). | 2 |
| [x] ~~**B21**~~ ✅ | `listByVaga` sem stageNotes/score (promovido de nota T-H05) | **FEITO** (2026-07-06). `VagaApplicationsService.listByVaga` agora inclui `generalScore`/`generalNote`/`stageNotes` na projeção (não entram no masking de RN-NOVA-03 — são avaliação interna, não dado de contato). `CandidateDrawer.vue` pré-carrega nota/score da etapa atual ao abrir (antes sempre abria em branco); `salvarNota()` agora emite `changed` (faltava — a lista pai nunca recebia a nota salva). `RadarQueryDto.order` ganhou `'salary'` (ordena por `salaryMax DESC NULLS LAST`), par do `order=fee` do B4. | 2 |

**Ordem recomendada no backend:** ~~B14 → B2/B7 → B6 → B1 → B3+B4 → B5+B8 → B19 → F1/F2/F9 → B17 → B18 → B16 (parcial) → B15 → B20 → B9~~ ✅ (núcleo + críticos de front + B9/B15/B16/B17/B18/B20 completos) → **B10 → B11 → B12/B13** → F10/F11/F4. (Nota: a delegação do B15 em si **não** depende de `activeContextTeamId`/F4 — é checada direto pela função/time real do ator, independente do toggle de contexto. Mas o F4 [WorkspaceSwitcher] continua sendo o que falta pra um MANAGER/RECRUITER de time *ver* as vagas dos colegas juntas na listagem hoje — isso sim usa `activeContextTeamId`, ver `VagasService.listMine`.) (F6/F8/F10-backend do §CABOS SOLTOS também já resolvidos fora de ordem, ver tabela.)

---

## Dívidas / Notas
- **[Auditoria 2026-07-05]** Varredura completa de cabos soltos feita a pedido do Andres (gatilho: logout invisível). Resultado consolidado em **§CABOS SOLTOS** (F1–F12) + gaps novos **B17–B21**. **[2026-07-05, mesma sessão] Críticos já resolvidos:** F1 (logout), F2 (conta candidato/empresa cai em 404 pós-login), F9 (CTA de preços quebrado), B19 (hash de senha vazando) — ver tabelas acima com ✅. F6 (footer 404) e F8 (fee=true, já estava ok) também resolvidos de passagem. E-mail deixado por último de propósito (a pedido do Andres) inicialmente; **liberado em 2026-07-06** ("no importa si tiene que ver con correo") — **B17 feito** nesse dia, resta só **F10** (página de front).
- `.claude/agents/` deve ser populado com `cp claude-setup/agents/*.md .claude/agents/` (sessão Cowork não pôde escrever em pasta protegida).
- Decidir nome/marca final (placeholder HUNTRIA; logo wordmark).
- **[B1, 2026-07-06] Migração `1749100000000-b1-user-personas` ainda não foi rodada** (`npm run migration:run`) — em dev a coluna `personas` já existe via `synchronize:true`, mas contas criadas antes do B1 ficam com `personas: null` até a migração rodar (backfill) ou até ativarem uma persona manualmente via `PATCH /profile/me/personas`. Rodar a migração antes de deploy em produção (lá `synchronize` é `false`).
- **[B1, 2026-07-06] `password` (hash) vazando em respostas de perfil autenticado** — `GET /profile/me`, `PATCH /profile` e o novo `PATCH /profile/me/personas` retornam a entidade `User` sem remover campos sensíveis (diferente de `getPublicProfile`/`getPublicCompany`, que já filtram). Não é regressão do B1 (o padrão já existia em `getMyProfile`/`updateProfile`), mas foi descoberto durante a validação E2E do B1. Vale um gap de limpeza dedicado — adicionar `select:false` em `password` (com `addSelect` só onde precisa, ex.: login) ou strip explícito no controller/service. **→ Promovido a B19 na auditoria de 2026-07-05.**
- Páginas pendentes referenciadas (T17 verificação e-mail, T18 utilitárias) ainda não existem — links já apontam para essas rotas. (T16 convite feita em 2026-07-05, ver B7; /app/escolher-perfil de T-C00 feita em 2026-07-06, ver F3.)
- **[B17, 2026-07-06] Duas contas de teste descartáveis ficaram no banco de dev** (`b17test...@getnada.com`, `b17resend...@getnada.com`) — sem endpoint de exclusão de usuário no backend pra limpar. Inofensivo (banco de teste), só registrando pra não confundir se aparecerem numa listagem de admin no futuro.
- **[B18, 2026-07-06] Mesma situação**: conta candidata descartável (`b18cand...@getnada.com`) ficou no banco (vaga+candidatura de teste foram deletadas, só a conta em si não tem como apagar).
- **[B16, 2026-07-06] Limitação de ambiente descoberta**: o sandbox do Claude não tem permissão de exclusão de arquivo na pasta conectada (`rm` retorna "Operation not permitted" mesmo pra um arquivo criado ali mesmo pra teste) — e mais grave, **um arquivo criado via `bash` nesse sandbox não aparece no sistema de arquivos real do Windows** (confirmado: `touch` via bash não apareceu num `Glob` do lado do Claude Code/host). Ou seja, o mount do bash pra essa pasta é uma cópia separada/dessincronizada, não o filesystem real — reforça a regra já anotada no CLAUDE.md de nunca confiar em leituras via bash logo após edições, e agora também: **nunca usar bash pra deletar ou verificar a existência de arquivos nesta pasta**. Consequência prática: `src/plans/plan-limit.guard.ts` ficou como arquivo órfão (sem nenhum import, inofensivo) — Andres precisa apagá-lo manualmente quando puder.
- **`qa-test-accounts.json` não tem conta de plano TEAM/ENTERPRISE** — falta para validar B7 (convite de time) ponta a ponta. Próxima vez que Andres tiver uma, adicionar lá.
- **B6 (2026-07-06): validado E2E completo.** Andres criou conta `isCompany` (`andresempresa@getnada.com`, username `andreshernandez9975`, empresa "ANDRES H TESTE" / tecnologia) e já estava logada no Chrome — `/empresa/andreshernandez9975` renderiza os dados reais e o estado vazio de vagas corretamente. Falta só testar a listagem `vagasAbertas` com uma vaga publicada de verdade (sem workspace Empresa ainda para isso).
- **B2 (2026-07-05): validado E2E completo.** Andres abriu o e-mail de reset e redefiniu a senha da conta `testeia@getnada.com` de verdade (nova senha salva em `qa-test-accounts.json`). Fluxo forgot-password → e-mail real → reset-password confirmado ponta a ponta.
- **B7: ainda falta validar E2E completo** — precisa de uma conta com plano TEAM/ENTERPRISE (não existe em `qa-test-accounts.json`) para criar um convite de verdade e aceitá-lo logado como o convidado. Chrome MCP não consegue abrir `inboxes.com`/`getnada.com` (bloqueado) para conferir o e-mail visualmente.
- **B4 (2026-07-06): validado E2E completo**, ver linha da tabela acima. Vaga real usada no teste: `74398f4d-8ed9-42e1-9d66-a2107d3bee10` (slug `desenvolvedora-full-stack-pleno`, da conta `testeia@getnada.com`) — ficou com `feePercent=50`, `maxHunters=5` no estado final (restaurado após os testes de cap). A candidata "Ana Souza" (pool do hunter) ficou de volta na etapa `para_analisar` (mascarada) ao fim da validação. **Dívida**: cap de `maxHunters` só foi validado via `@Min(1)` bloqueando o valor 0 (proxy indireto) — o caminho "409 ao tentar aceitar um 2º hunter além do limite" precisa de uma 2ª conta hunter real para ser exercitado ponta a ponta; a lógica foi revisada por código e é direta (`count(ACCEPTED) >= vaga.maxHunters`).
- **[B4, 2026-07-06] Sem UI para configurar `hunterContactRevealStageOrder`** (preferência de mascaramento de contato) — o campo existe e funciona (`PATCH /profile`), mas só é ajustável via API direta até existir a tela "Conta" (FASE 4) ou o workspace Empresa (FASE 3).
- **[Bug corrigido, 2026-07-06] Badge de origem sempre "Direta" no kanban** (`pages/app/hunter/vagas/[id]/index.vue:129`, achado durante validação do B21). O card do candidato tinha `<UiBadge>Direta</UiBadge>` fixo, ignorando `app.source` — o `CandidateDrawer.vue` já mostrava certo ("Indicado por hunter") pro mesmo campo, só o kanban estava hardcoded. Corrigido pra `v-if="app.source === 'HUNTER'"` (mesmo padrão do drawer). **Validado E2E completo**: com "Ana Souza" (source real `HUNTER`, confirmado via API) o card já mostra "Indicado por hunter"; criada uma 2ª candidatura de teste **direta** (conta candidato descartável aplicando via `POST /vagas/:slug/apply`) pra confirmar o outro branch — card mostrou "Direta" corretamente, os dois lado a lado na coluna "Para analisar". Candidatura de teste removida ao final (`DELETE /applications/:id`, 204); vaga restaurada a 1 candidato.
- **B21 (2026-07-06): validado E2E completo no Chrome.** Login real (`testeia@getnada.com`) → pipeline da vaga `desenvolvedora-full-stack-pleno` → drawer de "Ana Souza" → nota+score salvos (8,5 + observação de teste) → toast "Nota salva" → **reload completo da página** (força refetch de `listByVaga`) → reabrir o drawer já mostrou a nota pré-carregada (antes ficava sempre em branco). `order=salary` testado direto via fetch em `/vagas/radar` — 200 OK, sem erro de validação do enum. Nota de teste limpa ao final (`observacoes:'', nota:null`) para não deixar residual na candidata real. **Achado de passagem — já corrigido e validado (ver nota abaixo, mesmo dia)**: a candidata aparecia com badge "Direta" no kanban mesmo com `source:"HUNTER"`.
- **F12 (2026-07-06): validado no Chrome.** `/termos`, `/privacidade` e `/cookies` renderizam completos (TOC lateral + todas as seções), sem placeholder residual. Banner de cookies confirmado linkando corretamente para `/cookies`.
- **F5 (2026-07-06): validado E2E completo no Chrome.** Clique no sino abre popover "Nada por aqui ainda" com texto explicativo; clique fora fecha (click-outside); sem erros no console.
- **F3 (2026-07-06): validado E2E completo no Chrome.** Conta descartável criada via `/auth/register` sem `persona` (default do backend já é CANDIDATO — B1), logada, `/app` redirecionou sozinho pra `/app/candidato` (confirma F2 também). Acesso direto a `/app/escolher-perfil` mostrou "Sou profissional" com badge "já ativo" e "Sou recrutador/hunter" disponível; clique em "Ativar e entrar" chamou `activatePersona('HUNTER')` de verdade e navegou pro workspace hunter real (`Publicações do ciclo 0/1`, slot novo). Confirmado por `GET /profile/me` que `personas` acumulou `["CANDIDATO","HUNTER"]` e `vp_last_workspace` gravou `/app/hunter`. Reabrindo a página, as duas cards já aparecem "já ativo".
- **F7 (2026-07-06): validado E2E completo no Chrome.** Criado item de portfólio real via API (conta `testeia@getnada.com`, slug `projeto-teste-f7-v2`), publicado (`status:PUBLISHED`), acessado em `/portfolio/projeto-teste-f7-v2` (título/subtítulo/meta/autor OK), navegação `perfil → card portfólio → detalhe → "Ver perfil" → perfil` testada nos dois sentidos. Confirmado por `fetch` sem nenhum header de auth que a resposta pública não expõe `password`. Item de teste removido ao final (`DELETE /portfolio/:id`, 200). **Bônus**: tentativa de deletar o item com o token errado (conta empresa tentando deletar item criado por engano nela mesma, mas com a hunter logada) retornou 403 — confirma que o guard de ownership do endpoint funciona.
- **B5+B8 (2026-07-06): validado E2E completo.** Ciclo inteiro no Chrome: `/hunters` → `/hunter/[username]` → `/app/hunter/perfil` (self-edit + upload PDF + solicitar verificação) → `/app/admin/verificacoes` (recusar com motivo → hunter reenvia → aprovar) → selo "✓ Verificado" confirmado no diretório e perfil, gates de marketplace/candidatos desbloqueados. Conta admin criada via cadastro normal + `UPDATE users SET role='ADMIN'` direto no Postgres (não existe rota de auto-promoção) — credenciais em `qa-test-accounts.json`. **3 bugs reais encontrados e corrigidos nesta rodada** (detalhados nos CLAUDE.md de cada repo): (1) backend — `verifiedOnly=false` na query ignorado por causa de `Boolean("false")===true` no class-transformer, corrigido com `@Transform` explícito; (2) backend — perfil público sempre 404 porque o `select` de `getPublicProfile` esquecia o campo `personas`, usado no check de "é hunter?"; (3) frontend — campos de "Meu perfil" apareciam vazios em todo reload porque os refs locais eram inicializados antes do `auth.user` (populado assincronamente) resolver — risco real de perda de dados se salvasse sem perceber; corrigido com watcher `immediate` guardado por flag. **Dívidas**: (a) fila admin sem paginação/filtro (ok para o volume atual, só teve 1 pedido de cada vez); (b) `HunterMetrics` e ordenação `placements`/`rating` continuam proxy até B9/B10 existirem (mesmo padrão de `vagas/radar`); (c) migração `1749300000000-b5-b8-hunter-profile-verification` ainda não rodada em prod (dev usa `synchronize:true`).
